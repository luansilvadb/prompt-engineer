# Checklist

- [x] `import logging` está presente nos imports de `src/services.py`
- [x] `logger = logging.getLogger(__name__)` está definido após os imports
- [x] Nenhuma outra ocorrência de `logger` não definido existe no arquivo
