---
wave: 1
depends_on: []
files_modified:
  - src/teleprompter.py
  - src/signatures.py
autonomous: true
---

# Phase 3: Close gap: JUD-01, JUD-02 Fix optimizer.py to target Mode B and align teleprompter with medir_drift

## Requirements Covered
- JUD-01
- JUD-02

## Goal
Corrigir a quebra de integração introduzida na Fase 2, garantindo que o ecossistema (MCTS no `optimizer.py` e o compilador no `teleprompter.py`) passe a usar e mirar o `AvaliadorModoB` (Modo B - Caça-Defeitos) ao invés do Modo A.

## Tasks

<task>
<read_first>
- src/teleprompter.py
</read_first>
<action>
Modify `src/teleprompter.py` to compile and persist the Mode B judge.
1. Update imports: replace `AvaliadorDeSkill` with `AvaliadorModoB` from `src.signatures`.
2. Update the module instantiation: replace `avaliador_module = dspy.Predict(AvaliadorDeSkill)` with `avaliador_module = dspy.Predict(AvaliadorModoB)`.
3. Update all file paths: change `avaliador_modo_a_otimizado.json`, `avaliador_modo_a_otimizado.candidate.json`, and `avaliador_modo_a_otimizado.json.bak` to use `modo_b` instead of `modo_a`.
</action>
<acceptance_criteria>
- `grep "AvaliadorDeSkill" src/teleprompter.py` returns no results.
- `grep "avaliador_modo_a" src/teleprompter.py` returns no results.
- `grep "avaliador_modo_b" src/teleprompter.py` returns the expected JSON file names.
- `src/teleprompter.py` explicitly uses `AvaliadorModoB` for the compiler.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/signatures.py
</read_first>
<action>
Modify `funcao_de_recompensa` in `src/signatures.py` to use Mode B and penalize defects.
1. Replace the call to `_invoke_judge(exemplo, predicao)` with `_invoke_judge_modo_b_with(avaliador_modo_b_module, exemplo, predicao)`.
2. Modify the score return logic: check if `resultado.defeitos_encontrados` is not empty. If there are defects, calculate a penalty (e.g., `len(resultado.defeitos_encontrados) * 0.1`) and subtract it from the `score` (clamping minimum to 0.0).
3. If defects exist, format the returned `feedback` string by prepending "DEFEITOS E CONTRADIÇÕES ENCONTRADAS:\n", joining the list of defects with bullet points, and appending a strict warning message like "\n\nCorrija as quebras arquiteturais acima urgentemente."
4. If no defects exist, return the normal `score` and `resultado.feedback_detalhado`.
</action>
<acceptance_criteria>
- `funcao_de_recompensa` does not contain `_invoke_judge(` and instead contains `_invoke_judge_modo_b_with(`.
- Score penalty logic is implemented based on the length of `resultado.defeitos_encontrados`.
- Feedback formatting for Mode B defects includes the string "DEFEITOS E CONTRADIÇÕES ENCONTRADAS".
</acceptance_criteria>
</task>

## must_haves

```yaml
must_haves:
  truths:
    - D-01: src/teleprompter.py compiles and saves the model to avaliador_modo_b_otimizado.json
    - D-02: funcao_de_recompensa invokes _invoke_judge_modo_b_with and penalizes score if defeitos_encontrados exists
  prohibitions:
    - statement: src/teleprompter.py must not contain any reference to "modo_a"
      status: resolved
      verification: grep -i "modo_a" src/teleprompter.py returns no results
```

## Artifacts this phase produces
- `src/outputs/models/avaliador_modo_b_otimizado.json` (new file path)
- `src/outputs/models/avaliador_modo_b_otimizado.candidate.json` (new file path)
- `src/outputs/models/avaliador_modo_b_otimizado.json.bak` (new file path)

## Decision Tracking
- D-01
- D-02
