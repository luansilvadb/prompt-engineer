---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/semantic_evaluator.py
  - tests/test_semantic_evaluator.py
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - COGN-02
must_haves:
  truths:
    - Múltiplas requisições ao Avaliador Semântico não disparam a recarga do modelo em memória
    - Testes unitários verificam que textos similares recebem penalidade de decaimento contínuo
  artifacts:
    - src/semantic_evaluator.py
    - tests/test_semantic_evaluator.py
  key_links:
    - O arquivo de testes importa e invoca calculate_semantic_penalty de src/semantic_evaluator.py
---

<objective>
Implementar a utilidade isolada do Avaliador de Profundidade Semântica e instalar suas dependências.

Purpose: Isolar a responsabilidade de carregamento do modelo sentence-transformers e cálculo de similaridade, conforme arquitetura definida.
Output: Utilitário src/semantic_evaluator.py testado e dependências instaladas.

### Artifacts this phase produces
- src/semantic_evaluator.py
- tests/test_semantic_evaluator.py
- .planning/codebase/STACK.md
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-avaliador-de-profundidade-sem-ntica/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar Semantic Evaluator Singleton e Dependências</name>
  <files>src/semantic_evaluator.py, tests/test_semantic_evaluator.py, .planning/codebase/STACK.md</files>
  <read_first>
    - .planning/phases/04-avaliador-de-profundidade-sem-ntica/04-PATTERNS.md
    - .planning/phases/04-avaliador-de-profundidade-sem-ntica/04-RESEARCH.md
  </read_first>
  <behavior>
    - test_singleton_loading: Garantir que instâncias subsequentes do modelo retornam a mesma referência de memória.
    - test_no_penalty: Textos sem semelhança semântica (cosine sim <= 0.85) devem retornar fator de penalidade exatamente 1.0.
    - test_continuous_decay: Textos similares (cosine sim > 0.85) recebem fator de decaimento inversamente proporcional à similaridade (entre 0.01 e 1.0).
    - test_max_penalty: Textos idênticos retornam fator exato de 0.01.
  </behavior>
  <action>
    Instalar as dependências via comando exato `pip install sentence-transformers torch` como passo prévio ao TDD. 
    Atualizar o arquivo `.planning/codebase/STACK.md` adicionando 'sentence-transformers' e 'torch' na lista de dependências `## Dependencies`.
    Criar o arquivo `src/semantic_evaluator.py` implementando o singleton global `_embedder = None`. Se None, carregar `SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2')`.
    Criar função `calculate_semantic_penalty(text1: str, text2: str, threshold: float = 0.85)`: truncar text1 e text2 para 2048 caracteres; converter para tensor via `encode(..., convert_to_tensor=True)`; extrair similaridade com `util.cos_sim(emb1, emb2).item()`; se <= threshold retornar 1.0; se > threshold retornar `max(0.01, float(1.0 - ((similaridade - threshold) / (1.0 - threshold)) ** 2))`.
  </action>
  <verify>
    <automated>pytest tests/test_semantic_evaluator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - O comando `pytest tests/test_semantic_evaluator.py -v` passa sem falhas em todas as 4 asserts descritas no behavior.
    - O pacote sentence-transformers está instalado localmente e constando em STACK.md.
  </acceptance_criteria>
  <done>
    Todas as condições do TDD e dependências instaladas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NLP Engine | Onde instruções geradas pelo LLM (untrusted) entram no PyTorch para conversão em tensores |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | src/semantic_evaluator.py | medium | mitigate | Truncar `text1` e `text2` em no máximo 2048 caracteres antes do `.encode()` no sentence-transformers. |
| T-04-SC | Tampering | pip installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- A suite `tests/test_semantic_evaluator.py` roda com sucesso sem timeout.
</verification>

<success_criteria>
Utilitário implementado corretamente cobrindo as mecânicas semânticas usando o modelo multilingue exigido, sem acoplar ao framework MCTS ainda.
</success_criteria>

<output>
Create `.planning/phases/04-avaliador-de-profundidade-sem-ntica/04-01-SUMMARY.md` when done
</output>
