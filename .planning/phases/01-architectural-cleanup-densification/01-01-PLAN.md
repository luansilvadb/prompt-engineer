---
phase: 01-architectural-cleanup-densification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/drift/exceptions.py
  - src/drift/models.py
autonomous: true
requirements: [ARC-03]
must_haves:
  truths:
    - "src/drift/exceptions.py defines DriftMeasurementError(message, context) with identical signature/behavior to the current src/drift_monitor.py L44-50 class"
    - "src/drift/models.py defines every dataclass currently at src/drift_monitor.py L57-148 with the same fields, immutability decorators, and classmethods"
    - "ProbeExpectation.composite_score still delegates to src.signatures.calcular_composite (the one external cross-module link)"
    - "Neither new file creates an __init__.py (repo invariant: zero __init__.py files)"
  artifacts:
    - src/drift/exceptions.py
    - src/drift/models.py
  key_links:
    - "models.ProbeExpectation.composite_score() -> src.signatures.calcular_composite (preserve this import verbatim)"
---

<objective>
Create the foundation of the new `src/drift/` namespace package: the domain exception module and the data-model module. These two are the dependency-free leaves that every other drift service module will import from.

Purpose: First step of the ARC-03 densification (CONTEXT.md D-01: "Criar subpastas lógicas contendo vários arquivos pequenos"). Establishes the data + error vocabulary that golden/runner/metrics/gate depend on, so later plans import from a stable home.
Output: Two new files under `src/drift/` (namespace package — NO `__init__.py`).
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architectural-cleanup-densification/01-CONTEXT.md
@.planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
</context>

<deviation_note>
**IMPORTANT — corrected path vs. 01-PATTERNS.md (mutations side only).** The Pattern Map proposes `src/mutations/` as a new package while keeping `src/mutations.py` as a shim. Verified empirically (Python file vs. same-named directory conflict): a `.py` module file shadows a same-named directory, so `from src.mutations.registry import ...` would raise `ImportError`. The `src/drift/` package has NO such conflict (`drift_monitor.py` != `drift/`), so drift keeps the PATTERNS path. The mutations package is renamed to `src/mutation_strategies/` (handled in plan 01-02). This honors locked decision D-01 (densify into logical subfolders) and the repo invariant of zero `__init__.py` files. This plan (01-01) is unaffected — it only creates `src/drift/` files.
</deviation_note>

<tasks>

<task type="auto">
  <name>Task 1: Extract DriftMeasurementError into src/drift/exceptions.py</name>
  <files>src/drift/exceptions.py</files>
  <read_first>
    - src/drift_monitor.py (lines 44-50 — the exact class to move; also note all raise sites at L330-333 and L355-358 to confirm the (message, context=) contract)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (section "src/drift/exceptions.py" + Shared Pattern #6 "Domain exception with context dict")
    - .planning/codebase/CONVENTIONS.md (Error Handling section)
  </read_first>
  <action>
    Create directory src/drift/ (it becomes a Python 3 implicit namespace package — DO NOT create src/drift/__init__.py; the repo has zero __init__.py files, confirmed via glob; src/routers/ is the analog).

    Create src/drift/exceptions.py containing exactly one class: DriftMeasurementError(Exception). Move the class verbatim from src/drift_monitor.py L44-50: constructor signature __init__(self, message: str, context: Optional[dict] = None), sets self.message and self.context (defaulting to {} when None), docstring preserved. Use absolute imports rooted at src: the only import is `from typing import Optional` from stdlib. No other symbols in this file. Single responsibility: domain exception only.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/exceptions.py contains `class DriftMeasurementError(Exception):`
    - Source assertion: src/drift/exceptions.py contains `def __init__(self, message: str, context: Optional[dict] = None):`
    - Source assertion: src/drift/exceptions.py contains `self.context = context or {}`
    - Source assertion: src/drift/exceptions.py does NOT contain any other class or def (single-class file)
    - Filesystem: src/drift/__init__.py does NOT exist
    - Import resolves: `python -c "from src.drift.exceptions import DriftMeasurementError; e=DriftMeasurementError('x', context={'a':1}); assert e.context=={'a':1}; print('ok')"` exits 0 and prints ok (run from D:\good so the `src` package root is on the path)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift.exceptions import DriftMeasurementError; e=DriftMeasurementError('m', context={'k':1}); assert e.message=='m' and e.context=={'k':1}; print('exceptions ok')"</automated>
  </verify>
  <done>src/drift/exceptions.py exists with a single DriftMeasurementError class preserving the (message, context=) contract; namespace package has no __init__.py; import resolves and behaves identically.</done>
</task>

<task type="auto">
  <name>Task 2: Extract all drift dataclasses into src/drift/models.py</name>
  <files>src/drift/models.py</files>
  <read_first>
    - src/drift_monitor.py (lines 57-148 — the full dataclass block; preserve every field name, type, default, and classmethod)
    - src/signatures.py (locate calcular_composite — the one cross-module dependency that ProbeExpectation.composite_score must keep delegating to)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (section "src/drift/models.py" + Shared Pattern #7 "Dataclass immutability convention")
    - src/drift/exceptions.py (Task 1 output — models.py does NOT need to import it, but the package must exist)
  </read_first>
  <action>
    Create src/drift/models.py. Move every dataclass currently at src/drift_monitor.py L57-148 with identical fields, types, defaults, and classmethods. The set is: ProbeExpectation, GoldenProbe, DimensionError, DriftReport, GateDecision, DriftThresholds, and ProbeMeasurement (include any drift-related dataclass present in that line range — if an additional one exists in L57-148, move it too).

    Immutability convention (Shared Pattern #7): decorate value objects ProbeExpectation, GoldenProbe, DimensionError, GateDecision, DriftThresholds with @dataclass(frozen=True). Decorate aggregators holding mutable list fields (DriftReport, ProbeMeasurement) with plain @dataclass and use field(default_factory=list) for list-typed fields. Preserve DriftReport.to_dict() and DriftThresholds.from_config(cls, cfg) classmethods verbatim on their owning classes.

    Imports: absolute, src-rooted. Required: `from typing import Optional, List` and `from dataclasses import dataclass, field` from stdlib. Cross-module link to preserve verbatim: `from src.signatures import calcular_composite` and ProbeExpectation.composite_score(self) returns calcular_composite(self). Do NOT use relative imports (`.models` style is banned repo-wide).

    Do not create src/drift/__init__.py.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/models.py contains `from src.signatures import calcular_composite`
    - Source assertion: src/drift/models.py contains `@dataclass(frozen=True)` for ProbeExpectation, GoldenProbe, DimensionError, GateDecision, DriftThresholds (5 frozen dataclasses)
    - Source assertion: src/drift/models.py contains `def composite_score(self) -> float:` whose body returns calcular_composite(self)
    - Source assertion: src/drift/models.py contains `def from_config(cls, cfg: dict)` and `def to_dict(self) -> dict`
    - Source assertion: src/drift/models.py contains NO relative import (grep for `from \.` returns 0 matches)
    - Filesystem: src/drift/__init__.py does NOT exist
    - Import resolves: `python -c "from src.drift.models import ProbeExpectation, GoldenProbe, DimensionError, DriftReport, GateDecision, DriftThresholds, ProbeMeasurement; print('models ok')"` exits 0 and prints models ok
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift.models import ProbeExpectation, GoldenProbe, DimensionError, DriftReport, GateDecision, DriftThresholds, ProbeMeasurement; t=DriftThresholds.from_config({'spearman_floor':0.8}); print(type(t).__name__, 'models ok')"</automated>
  </verify>
  <done>All drift dataclasses relocated to src/drift/models.py with fields/decorators/methods preserved; calcular_composite delegation intact; namespace package intact; import resolves.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
New symbols (relocated, not new logic):
- File: src/drift/exceptions.py
  - class DriftMeasurementError(Exception)
- File: src/drift/models.py
  - @dataclass(frozen=True) ProbeExpectation (with .composite_score())
  - @dataclass(frozen=True) GoldenProbe
  - @dataclass(frozen=True) DimensionError
  - @dataclass DriftReport (with .to_dict())
  - @dataclass(frozen=True) GateDecision
  - @dataclass(frozen=True) DriftThresholds (with .from_config())
  - @dataclass ProbeMeasurement
No behavior change — pure relocation. These symbols are excluded from drift-verification "new symbol" expectations (they are moves).
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries
This plan relocates existing in-process code into a sibling module within the same trust domain (src package). No new trust boundary is introduced; no new input source, no new I/O channel, no new external dependency.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | DriftMeasurementError.context | low | accept | The context dict already exists in the current code and is preserved verbatim; no new leakage surface introduced by moving the class to a sibling file. |
| T-01-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (pure stdlib relocation). No [ASSUMED]/[SUS] packages. |

Behavior-preserving refactor: each task's acceptance criteria includes an import-resolves check that guards against accidental API drift.
</threat_model>

<verification>
- python -c "from src.drift.exceptions import DriftMeasurementError" exits 0
- python -c "from src.drift.models import ProbeExpectation, GoldenProbe, DimensionError, DriftReport, GateDecision, DriftThresholds, ProbeMeasurement" exits 0
- No file named src/drift/__init__.py exists
- (These two new files are not yet wired into consumers — that happens in plan 01-05 when src/drift_monitor.py becomes a re-export shim. Until then the old drift_monitor.py keeps working unchanged.)
</verification>

<success_criteria>
- DriftMeasurementError and all drift dataclasses exist ONLY under src/drift/ in their new canonical home (old copies remain in drift_monitor.py until plan 01-05; duplication is intentional and temporary).
- Both new files import cleanly from a fresh Python process.
- calcular_composite delegation preserved.
- Namespace-package convention honored (no __init__.py).
</success_criteria>

<output>
Create `.planning/phases/01-architectural-cleanup-densification/01-01-SUMMARY.md` when done
</output>
