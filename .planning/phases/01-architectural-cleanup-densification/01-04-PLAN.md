---
phase: 01-architectural-cleanup-densification
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/drift/gate.py
  - src/drift/circuit_breaker.py
  - src/drift/cache.py
autonomous: true
requirements: [ARC-01, ARC-02, ARC-03]
must_haves:
  truths:
    - "src/drift/gate.py exposes DriftGate.avaliar_candidato with identical accept/reject decisions to the current implementation, minus the dead nested helper and minus the orphaned strict_required assignment"
    - "The dead nested comparison helper (defined-but-never-called inside DriftGate) is ABSENT from src/drift/gate.py and from the whole codebase after this plan"
    - "DriftGate.avaliar_candidato uses a single _gate_against_baseline_or_floor helper for both the Spearman and Offset steps (ARC-02), not a duplicated if/else block"
    - "src/drift/circuit_breaker.py exposes verificar_juiz_atual and circuit_breaker as module-level functions preserving the BR4 rollback-to-zero-judge behavior"
    - "src/drift/cache.py exposes load_drift_cache/save_drift_cache/_drift_cache_path with atomic write and resilient fallback preserved"
  artifacts:
    - src/drift/gate.py
    - src/drift/circuit_breaker.py
    - src/drift/cache.py
  key_links:
    - "gate imports DriftReport, GateDecision, DriftThresholds from src.drift.models"
    - "circuit_breaker imports GoldenSet (golden), JudgeProbeRunner (runner), medir_drift (metrics), DriftReport/GateDecision (models)"
    - "cache imports DriftReport from src.drift.models"
---

<objective>
Create the final drift service modules: the decision gate (DriftGate), the circuit-breaker (self-heal of a corrupted judge), and the drift cache (atomic persistence of the production DriftReport). This plan also performs the concrete ARC-01 dead-code deletion (the unused nested comparison helper inside DriftGate + its orphaned local assignment) and the ARC-02 de-duplication of the Spearman/Offset gate logic.

Purpose: Completes the drift densification (ARC-03), delivers the one verified dead-code deletion (ARC-01), and reduces the duplicated conditional block in avaliar_candidato (ARC-02).
Output: Three new files under src/drift/.
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architectural-cleanup-densification/01-CONTEXT.md
@.planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-architectural-cleanup-densification/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract DriftGate into src/drift/gate.py — delete dead helper (ARC-01) + de-duplicate gate logic (ARC-02)</name>
  <files>src/drift/gate.py</files>
  <read_first>
    - src/drift_monitor.py (lines 476-553 — DriftGate class; the dead nested comparison helper at L500-509; the orphaned local assignment at L498; the duplicated Spearman block L511-525 and Offset block L527-541)
    - Verify the dead-code finding before deleting: grep the entire repo for the helper name — it must have exactly ONE occurrence (its own definition) and the orphaned local must have no reader outside the deleted helper. If a reader exists, STOP and do not delete that symbol.
    - src/drift/models.py (plan 01-01 output — DriftReport, GateDecision, DriftThresholds are the types used)
    - src/drift/metrics.py (plan 01-03 output — only if avaliar_candidato references metrics; it should not, but confirm)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (section "src/drift/gate.py" + Shared Pattern #8 helper-functions-not-patterns)
  </read_first>
  <!-- planner-discipline-allow: _strict_better_or_reject -->
  <action>
    Create src/drift/gate.py. Relocate the DriftGate class from src/drift_monitor.py L476-553 as a class with the single @staticmethod avaliar_candidato(report_cand, report_atual, thresholds) -> GateDecision. Preserve the hierarchical early-return decision ordering and the `# Passo 1/2/3/4` comment headers (they document the gate-ordering invariant).

    ARC-01 (dead code) — OMIT two things that currently live inside avaliar_candidato (do NOT copy them into gate.py):
      (a) the unused nested comparison helper defined-but-never-called at current L500-509 (name: _strict_better_or_reject). Before omitting, grep the whole repo to confirm it has no caller — PATTERNS.md states 1 occurrence total.
      (b) the orphaned local assignment at current L498 (name: strict_required) IF, and only if, it has no remaining reader after (a) is removed. PATTERNS.md notes the final low-confidence message block reads report_cand.low_confidence directly, not this local — so the local is dead once (a) is gone. Verify by grep before deleting.

    ARC-02 (complexity) — the duplicated `if report_atual is not None: <regression-margin check> else: <absolute-floor check>` structure appears twice (Spearman ~L511-525 and Offset ~L527-541). Extract ONE module-level (or @staticmethod) private helper to collapse both:
      _gate_against_baseline_or_floor(cand_val, atual_val, threshold_abs, margin, better_when_lower, metric_name, fmt) -> Optional[GateDecision]
    returning a reject GateDecision when the candidate fails the baseline-margin or the absolute floor, else None. Then avaliar_candidato calls it once for Spearman and once for Offset. CONTEXT.md D-02 EXPLICITLY BANS OO Strategy/Policy classes here — this MUST be a plain function/staticmethod, never an ABC hierarchy.

    Imports: absolute src-rooted — `from src.drift.models import DriftReport, GateDecision, DriftThresholds`; stdlib typing (Optional). No relative imports. No __init__.py.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/gate.py contains `class DriftGate:` and `def avaliar_candidato(`
    - Source assertion: src/drift/gate.py contains `def _gate_against_baseline_or_floor(`
    - DEAD CODE GONE: `grep -c "_strict_better_or_reject" src/drift/gate.py` returns 0
    - DEAD CODE GONE repo-wide (after plan 01-05 also clears the old copy): for THIS plan, also confirm `grep -rn "_strict_better_or_reject" src/drift/` returns 0 matches
    - Source assertion: src/drift/gate.py contains the `# Passo` step headers (gate-ordering invariant preserved)
    - Source assertion: src/drift/gate.py does NOT define any ABC/Strategy/Policy class (grep `ABC`, `class .*Strategy`, `class .*Policy` return 0)
    - Source assertion: no relative imports (grep `from \.` returns 0)
    - Filesystem: src/drift/__init__.py does NOT exist
    - Import resolves: `python -c "from src.drift.gate import DriftGate; print('gate ok')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "import src.drift.gate as g; src=open('src/drift/gate.py',encoding='utf-8').read(); assert hasattr(g,'DriftGate'); assert '_strict_better_or_reject' not in src, 'dead helper leaked'; assert 'class DriftGate' in src and '_gate_against_baseline_or_floor' in src; print('gate ok')"</automated>
  </verify>
  <done>DriftGate relocated to gate.py; the dead nested helper and orphaned local are OMITTED (verified absent under src/drift/); the duplicated Spearman/Offset block collapsed into one _gate_against_baseline_or_floor helper (plain function, no OO pattern); gate ordering preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extract circuit_breaker and cache modules</name>
  <files>src/drift/circuit_breaker.py, src/drift/cache.py</files>
  <read_first>
    - src/drift_monitor.py (lines 560-607 for verificar_juiz_atual + circuit_breaker incl. the BR4 rollback rename-with-timestamp at L590-597; lines 614-652 for _drift_cache_path/load_drift_cache/save_drift_cache; also locate the MODELS_DIR constant definition used by both)
    - src/drift/golden.py, src/drift/runner.py, src/drift/metrics.py, src/drift/models.py (plan 01-01/01-03 outputs — the symbols circuit_breaker imports)
    - src/store.py (atomic-write idiom parity)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (sections "src/drift/circuit_breaker.py" and "src/drift/cache.py" + Shared Patterns #3 atomic persistence, #4 resilient fallback)
  </read_first>
  <action>
    Create src/drift/circuit_breaker.py as module-level functions (NOT a class — matches PATTERNS). Define the module constant MODELS_DIR = Path('src/outputs/models'). Relocate from src/drift_monitor.py L560-607:
      - verificar_juiz_atual(thresholds, repetitions) -> Optional[DriftReport]: build GoldenSet(); if empty return None; build JudgeProbeRunner('atual'); load candidate from MODELS_DIR/'avaliador_otimizado.json' if it exists else as_zero(); return medir_drift(...).
      - circuit_breaker(thresholds, repetitions) -> GateDecision: report = verificar_juiz_atual(...); if None return GateDecision(True, "golden ausente; nada a verificar", None); if report.critical_rules_violated perform the BR4 rollback (os.replace the model file to a timestamped backup, restoring the zero judge) — keep this rename-with-timestamp logic inline (it is small and already single-purpose); return GateDecision(...) describing the outcome.
    Imports absolute src-rooted: `from src.drift.golden import GoldenSet`; `from src.drift.runner import JudgeProbeRunner`; `from src.drift.metrics import medir_drift`; `from src.drift.models import DriftReport, GateDecision`; stdlib os, pathlib.Path, typing.Optional.

    Create src/drift/cache.py. Relocate from src/drift_monitor.py L614-652:
      - _drift_cache_path() -> Path: MODELS_DIR.mkdir(parents=True, exist_ok=True); return MODELS_DIR/'drift_cache.json'. (Import MODELS_DIR from src.drift.circuit_breaker, OR redefine it here as the same constant — pick ONE owner and import from it to avoid divergence; circuit_breaker.py is the owner, cache.py imports it.)
      - load_drift_cache() -> Optional[DriftReport]: path missing → None; try json.load → rebuild DriftReport(...); except Exception → None (resilient).
      - save_drift_cache(report) -> DriftReport's atomic write: temp path = path.with_suffix('.tmp'); json.dump(report.to_dict(), ensure_ascii=False, indent=2); os.replace(temp, path); on Exception print [!] and continue.
    Preserve the [!]/[*]/[+] console-prefix convention and the atomic temp+os.replace idiom. No relative imports. No __init__.py.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/circuit_breaker.py contains `def verificar_juiz_atual(` and `def circuit_breaker(` and `MODELS_DIR = Path('src/outputs/models')`
    - Source assertion: src/drift/circuit_breaker.py contains `from src.drift.golden import GoldenSet` and `from src.drift.runner import JudgeProbeRunner` and `from src.drift.metrics import medir_drift`
    - Source assertion: src/drift/cache.py contains `def load_drift_cache(`, `def save_drift_cache(`, and `def _drift_cache_path(`
    - Source assertion: src/drift/cache.py contains `os.replace(` (atomic write) and a `[!]` prefixed error log
    - Source assertion: no relative imports in either file (grep `from \.` returns 0)
    - Filesystem: src/drift/__init__.py does NOT exist
    - Import resolves: `python -c "from src.drift.circuit_breaker import verificar_juiz_atual, circuit_breaker; from src.drift.cache import load_drift_cache, save_drift_cache; print('cb+cache ok')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift.circuit_breaker import verificar_juiz_atual, circuit_breaker, MODELS_DIR; from src.drift.cache import load_drift_cache, save_drift_cache, _drift_cache_path; assert load_drift_cache() is None or hasattr(load_drift_cache(),'to_dict'); print('cb+cache ok', str(MODELS_DIR))"</automated>
  </verify>
  <done>circuit_breaker.py exposes the two module functions + MODELS_DIR with BR4 rollback preserved; cache.py exposes the three cache functions with atomic write + resilient fallback; both import cleanly.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
New symbols:
- File: src/drift/gate.py
  - class DriftGate (moved)
  - DriftGate.avaliar_candidato (moved, de-duplicated)
  - _gate_against_baseline_or_floor (NEW — ARC-02 collapse of duplicated Spearman/Offset block)
  - REMOVED: the dead nested comparison helper (ARC-01) and the orphaned local assignment (ARC-01)
- File: src/drift/circuit_breaker.py
  - MODELS_DIR (module constant, moved — canonical owner)
  - verificar_juiz_atual (moved)
  - circuit_breaker (moved)
- File: src/drift/cache.py
  - _drift_cache_path (moved)
  - load_drift_cache (moved)
  - save_drift_cache (moved)
External decision behavior of DriftGate is identical (same accept/reject outcomes); only dead code is removed and internal structure simplified.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries
Relocation within src. circuit_breaker performs a filesystem rollback (model file → timestamped backup) on detecting a corrupted judge — this destructive-on-disk operation pre-exists and is preserved verbatim (BR4 self-heal). cache.py reads/writes local JSON. No new boundary.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | circuit_breaker BR4 rollback (os.replace of model file) | medium | mitigate | Preserve the timestamped-backup-before-replace ordering verbatim so a corrupted judge is never lost without a recoverable backup. Acceptance criteria confirm circuit_breaker is relocated intact. |
| T-01-06 | Repudiation | dead-code removal in gate.py | low | mitigate | The deleted nested helper is verified-unused via repo-wide grep before omission (read_first step + acceptance criterion `_strict_better_or_reject` absent under src/drift/). No callable code is removed. |
| T-01-SC | Tampering | package installs | low | accept | No package-manager installs (stdlib relocation only). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- python -c "from src.drift.gate import DriftGate" exits 0 AND src/drift/gate.py contains no occurrence of the deleted dead helper name
- python -c "from src.drift.circuit_breaker import verificar_juiz_atual, circuit_breaker" exits 0
- python -c "from src.drift.cache import load_drift_cache, save_drift_cache" exits 0
- No file named src/drift/__init__.py exists
- Repo-wide grep for the dead helper name under src/drift/ returns 0 (the old copy in drift_monitor.py is cleared in plan 01-05)
</verification>

<success_criteria>
- DriftGate relocated with identical decision outcomes; dead nested helper + orphaned local omitted (ARC-01); duplicated block collapsed into one helper, no OO pattern (ARC-02).
- circuit_breaker + cache modules created with BR4 rollback and atomic-write/resilient-fallback behavior preserved.
- All imports resolve.
</success_criteria>

<output>
Create `.planning/phases/01-architectural-cleanup-densification/01-04-SUMMARY.md` when done
</output>
