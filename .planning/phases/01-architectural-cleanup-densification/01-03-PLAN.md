---
phase: 01-architectural-cleanup-densification
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/drift/golden.py
  - src/drift/runner.py
  - src/drift/metrics.py
autonomous: true
requirements: [ARC-02, ARC-03]
must_haves:
  truths:
    - "src/drift/golden.py exposes GoldenSet with identical public behavior to src/drift_monitor.py L155-248, including the load-bearing PyInstaller frozen-executable fallback"
    - "GoldenSet._load() is flattened to a sequence of early-returns via _restore_frozen_golden and _parse_golden_json helpers (ARC-02)"
    - "src/drift/runner.py exposes JudgeProbeRunner preserving the per-instance DSPy docstring and the DriftMeasurementError(context=...) contract"
    - "src/drift/metrics.py exposes _spearman_rank_correlation (with inner ranks closure promoted to module-level _compute_ranks) and medir_drift flattened via four helper functions"
    - "No __init__.py created"
  artifacts:
    - src/drift/golden.py
    - src/drift/runner.py
    - src/drift/metrics.py
  key_links:
    - "golden imports ProbeExpectation, GoldenProbe from src.drift.models and DriftMeasurementError from src.drift.exceptions"
    - "runner imports ProbeMeasurement from src.drift.models, GoldenProbe from src.drift.models, DriftMeasurementError from src.drift.exceptions, and AvaliadorDeSkill/Avaliacao/_invoke_judge_with from src.signatures"
    - "metrics imports DriftReport, DimensionError, GoldenProbe from src.drift.models and DriftMeasurementError from src.drift.exceptions"
---

<objective>
Create the first wave of drift service modules: the GoldenSet loader (file I/O with frozen-exe fallback), the JudgeProbeRunner (LLM probe execution), and the pure metrics module (Spearman + drift measurement). These three depend only on the foundation modules created in plan 01-01 (exceptions, models). They are mutually independent (none imports the others), so a single agent creates them efficiently in one context.

Purpose: ARC-03 densification continues; ARC-02 cyclomatic-complexity reduction is applied to GoldenSet._load(), _spearman_rank_correlation (inner closure), and medir_drift (~70-line body) per CONTEXT.md D-02 (small helpers, no OO patterns).
Output: Three new files under src/drift/.
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architectural-cleanup-densification/01-CONTEXT.md
@.planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-architectural-cleanup-densification/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract GoldenSet into src/drift/golden.py with ARC-02 helper extraction in _load()</name>
  <files>src/drift/golden.py</files>
  <read_first>
    - src/drift_monitor.py (lines 155-248 — GoldenSet class; focus _load() L171-207 with its 4 nested branches, the frozen PyInstaller block L174-184, the probe parsing loop L193-203, and save() L218-248)
    - src/store.py (atomic-write idiom — temp + os.replace, for parity)
    - src/drift/models.py (plan 01-01 output — ProbeExpectation, GoldenProbe are the types used here)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (section "src/drift/golden.py" + Shared Patterns #3 atomic persistence, #4 resilient fallback, #5 frozen-executable detection)
  </read_first>
  <action>
    Create src/drift/golden.py. Move the GoldenSet class and its module-level constant GOLDEN_DIR = Path('src/outputs/golden') from src/drift_monitor.py L155-248.

    Preserve exactly: __init__ (sets self.version, self.probes=[], calls self._load()); _store_path() (mkdir parents+exist_ok, returns GOLDEN_DIR/'golden_set.json'); save(version, curated_at) with the atomic temp+os.replace write and the BR3 docstring "Persistência atômica — USAR APENAS EM CURADORIA OFFLINE".

    ARC-02 refactor of _load() (the 4 nested branches: exists / frozen / re-exists / try). Extract two private methods on GoldenSet so _load() becomes a flat sequence of early-returns (per Shared Pattern #8 — small helpers, NOT OO patterns; CONTEXT.md D-02 bans OO overengineering):
      - _restore_frozen_golden(self, path) -> Path — encapsulates the PyInstaller sys._MEIPASS fallback block (current L174-184). KEEP this block verbatim including `import sys` and the `getattr(sys, 'frozen', False) and hasattr(sys, '_MEIPASS')` check — it is load-bearing for the packaged desktop build (desktop.py).
      - _parse_golden_json(self, data) -> List[GoldenProbe] — the probe parsing loop (current L193-203), building GoldenProbe instances from the loaded dict (ProbeExpectation(**pd['expected']) etc.).
    After extraction _load() reads: compute path; if not path.exists() → resilient fallback (probes=[], [!] log); if frozen → path = self._restore_frozen_golden(path); load json; self.probes = self._parse_golden_json(data); resilient try/except around json load.

    Imports: absolute, src-rooted — `from src.drift.models import ProbeExpectation, GoldenProbe` and `from src.drift.exceptions import DriftMeasurementError` (only if the current code raises it here; otherwise omit). stdlib: json, os, sys, pathlib.Path, typing. No relative imports. No __init__.py.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/golden.py contains `class GoldenSet:` and `GOLDEN_DIR = Path('src/outputs/golden')`
    - Source assertion: src/drift/golden.py contains `def _restore_frozen_golden(self` AND `def _parse_golden_json(self`
    - Source assertion: src/drift/golden.py contains `getattr(sys, 'frozen', False)` AND `'_MEIPASS'` (frozen-exe fallback preserved)
    - Source assertion: src/drift/golden.py contains `os.replace(` (atomic write in save())
    - Source assertion: src/drift/golden.py contains `from src.drift.models import` (absolute import)
    - Source assertion: grep for relative imports `from \.` returns 0 matches in this file
    - Filesystem: src/drift/__init__.py does NOT exist
    - Import resolves: `python -c "from src.drift.golden import GoldenSet, GOLDEN_DIR; g=GoldenSet(); print('golden ok', g.version)"` exits 0 (run from D:\good)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift.golden import GoldenSet, GOLDEN_DIR; g=GoldenSet(); assert hasattr(g,'probes'); print('golden ok')" </automated>
  </verify>
  <done>GoldenSet relocated to src/drift/golden.py with _load() flattened via two helpers, frozen-exe fallback preserved verbatim, atomic save preserved; imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extract JudgeProbeRunner and the metrics module</name>
  <files>src/drift/runner.py, src/drift/metrics.py</files>
  <read_first>
    - src/drift_monitor.py (lines 314-359 for JudgeProbeRunner incl. the load-bearing per-instance DSPy docstring at L315-319; lines 366-469 for _spearman_rank_correlation and medir_drift)
    - src/signatures.py (AvaliadorDeSkill, Avaliacao, _invoke_judge_with — the DSPy types/functions runner depends on)
    - src/drift/models.py (plan 01-01 output — ProbeMeasurement, GoldenProbe, DriftReport, DimensionError are the types used)
    - src/drift/golden.py (Task 1 output — only for GoldenProbe type; runner references GoldenProbe in its run() signature, but GoldenProbe is defined in models.py, import it from there)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (sections "src/drift/runner.py" and "src/drift/metrics.py" + Shared Pattern #6 domain-exception-with-context, #8 helper-functions-not-patterns)
  </read_first>
  <action>
    Create src/drift/runner.py. Move the JudgeProbeRunner class from src/drift_monitor.py L314-359 verbatim. Preserve: __init__(self, label) creating a per-instance self._judge = dspy.Predict(AvaliadorDeSkill); load_candidate(path) (try/except → raise DriftMeasurementError with context={'judge_label', 'path', 'original'}); as_zero(); run(probe, repetitions) -> ProbeMeasurement (loop → _invoke_judge_with(...) → samples; raise DriftMeasurementError if 0 samples, context includes probe_id and failures). PRESERVE the per-instance docstring at current L315-319 verbatim — it documents that .demos is per-instance (load-bearing DSPy isolation, Norma 3 / R2). Imports: `import dspy`; absolute src-rooted — `from src.signatures import AvaliadorDeSkill, Avaliacao, _invoke_judge_with`; `from src.drift.exceptions import DriftMeasurementError`; `from src.drift.models import ProbeMeasurement, GoldenProbe`.

    Create src/drift/metrics.py (pure functions, no class — config.py is the analog for the no-state pure-function style). Two public functions relocated from src/drift_monitor.py L366-469:
      1. _spearman_rank_correlation(x, y) -> float — relocate, but ARC-02 promote the inner ranks(values) closure (current L376-388) to a MODULE-LEVEL private function _compute_ranks(values: List[float]) -> List[float]. The nested while/while becomes a flat standalone helper, dropping the parent's cyclomatic complexity.
      2. medir_drift(runner, golden, repetitions, thresholds) -> DriftReport — relocate and flatten the ~70-line body (current L400-469) into a flat compose-and-return by extracting four module-level private helpers (per Shared Pattern #8, NOT OO):
         - _measure_all_probes(runner, golden, repetitions) -> list
         - _compute_mae_per_dimension(measurements, dims) -> List[DimensionError]
         - _compute_concordance_and_violations(measurements) -> tuple (concordance, total_missed, total_false_rej)
         - _build_per_probe(measurements) -> List[dict]
         After extraction medir_drift reads: raise if golden.is_empty(); measurements = _measure_all_probes(...); compute spearman via _spearman_rank_correlation; mae = _compute_mae_per_dimension(...); concordance/violations = _compute_concordance_and_violations(...); per_probe = _build_per_probe(...); return DriftReport(...). Preserve the early `raise DriftMeasurementError("Golden set vazio — nada a medir.")` guard.
    Keep the `_` prefix on all internal helpers (project convention). Imports: absolute src-rooted — `from src.drift.models import DriftReport, DimensionError, GoldenProbe` and `from src.drift.exceptions import DriftMeasurementError`; stdlib typing. No relative imports. No __init__.py.
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift/runner.py contains `class JudgeProbeRunner:` and `self._judge = dspy.Predict(AvaliadorDeSkill)`
    - Source assertion: src/drift/runner.py contains `raise DriftMeasurementError(` (error-context contract preserved)
    - Source assertion: src/drift/metrics.py contains `def _spearman_rank_correlation(x` and `def _compute_ranks(values` and `def medir_drift(`
    - Source assertion: src/drift/metrics.py contains the four helpers `def _measure_all_probes(`, `def _compute_mae_per_dimension(`, `def _compute_concordance_and_violations(`, `def _build_per_probe(`
    - Source assertion: src/drift/metrics.py contains `raise DriftMeasurementError("Golden set vazio` (empty-golden guard preserved)
    - Source assertion: neither file contains a relative import (grep `from \.` returns 0)
    - Source assertion: neither file defines an ABC/Strategy/Policy class (grep `ABC`, `class .*Strategy` return 0)
    - Import resolves: `python -c "from src.drift.runner import JudgeProbeRunner; from src.drift.metrics import medir_drift, _spearman_rank_correlation; r=_spearman_rank_correlation([1,2,3],[3,2,1]); print('spearman', round(r,3))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift.runner import JudgeProbeRunner; from src.drift.metrics import medir_drift, _spearman_rank_correlation, _compute_ranks; assert abs(_spearman_rank_correlation([1,2,3,4],[1,2,3,4]) - 1.0) < 1e-9; assert _spearman_rank_correlation([1],[2]) == 1.0; print('metrics ok')"</automated>
  </verify>
  <done>JudgeProbeRunner relocated with per-instance docstring + error contract preserved; metrics module exposes spearman (with _compute_ranks helper) and a flattened medir_drift (four helpers); all imports resolve; spearman correctness sanity-checked on known inputs.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
New symbols:
- File: src/drift/golden.py
  - GOLDEN_DIR (module constant, moved)
  - class GoldenSet (moved)
  - GoldenSet._restore_frozen_golden (NEW — ARC-02 extraction from _load)
  - GoldenSet._parse_golden_json (NEW — ARC-02 extraction from _load)
- File: src/drift/runner.py
  - class JudgeProbeRunner (moved)
- File: src/drift/metrics.py
  - _spearman_rank_correlation (moved)
  - _compute_ranks (NEW — ARC-02 promotion of inner closure to module level)
  - medir_drift (moved)
  - _measure_all_probes (NEW — ARC-02)
  - _compute_mae_per_dimension (NEW — ARC-02)
  - _compute_concordance_and_violations (NEW — ARC-02)
  - _build_per_probe (NEW — ARC-02)
External behavior unchanged; only internal structure simplified.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries
Relocation within the src trust domain. runner.py makes LLM calls (via dspy / src.signatures._invoke_judge_with) and golden.py reads local JSON — both surfaces pre-exist and are preserved with identical behavior, including the DriftMeasurementError(context=...) contract that avoids leaking raw stack traces into control flow.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | LLM/probe error context | low | accept | DriftMeasurementError context-dict contract preserved verbatim; no new secret or PII handling introduced by the move. |
| T-01-04 | Denial of Service | medir_drift control flow | low | accept | Behavior-preserving refactor; the empty-golden guard and per-probe error raising are preserved. Acceptance criteria confirm the guard remains. |
| T-01-SC | Tampering | package installs | low | accept | No package-manager installs (stdlib + existing dspy import only). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- python -c "from src.drift.golden import GoldenSet, GOLDEN_DIR; GoldenSet()" exits 0
- python -c "from src.drift.runner import JudgeProbeRunner" exits 0
- python -c "from src.drift.metrics import medir_drift, _spearman_rank_correlation, _compute_ranks" exits 0
- Spearman sanity check passes (identity=1.0, n<2=1.0)
- No file named src/drift/__init__.py exists
</verification>

<success_criteria>
- Three drift service modules created, each single-responsibility.
- ARC-02 applied: _load flattened (2 helpers), spearman inner closure promoted (1 helper), medir_drift flattened (4 helpers) — all as plain functions/methods, no OO patterns.
- Frozen-executable fallback and DriftMeasurementError context contract preserved verbatim.
- All imports resolve from a fresh process.
</success_criteria>

<output>
Create `.planning/phases/01-architectural-cleanup-densification/01-03-SUMMARY.md` when done
</output>
