---
plan: 06-03
phase: 06
slug: mutador-cognitivo
title: "Wave 0 Test Stubs e Verificação de Regressão"
wave: 3
depends_on:
  - 06-01
  - 06-02
requirements:
  - COGN-01
files_modified:
  - tests/test_signatures.py
  - tests/test_registry.py
  - tests/test_bandit.py
  - tests/test_config.py
  - tests/test_optimizer_integration.py
  - tests/test_optimizer.py
autonomous: true
estimated_tasks: 3
---

# Plan 06-03 — Wave 0 Test Stubs e Verificação de Regressão

## Goal

Criar os arquivos de teste necessários para cobertura do COGN-01, adicionar testes de routing intercept ao `test_optimizer.py` existente, e garantir que toda a suite passa verde após as mudanças dos planos 06-01 e 06-02.

## Context

Este plano depende de 06-01 e 06-02. Os arquivos `tests/test_signatures.py`, `tests/test_registry.py`, `tests/test_bandit.py`, `tests/test_config.py` e `tests/test_optimizer_integration.py` NÃO existem — devem ser criados. O arquivo `tests/test_optimizer.py` JÁ EXISTE e deve ser estendido (não sobrescrito) com novos testes de routing.

Todos os testes desta fase rodam SEM LLM. O DSPy é mockado onde necessário. A validação de `MutadorCognitivoAgent` é puramente estrutural (field inspection). O integration smoke test usa `Optimizer.__init__` com todos os módulos de LLM mockados.

## Artifacts This Phase Produces

| Symbol | File | Type |
|--------|------|------|
| `test_mutador_cognitivo_agent_fields` | `tests/test_signatures.py` | pytest test function |
| `test_raciocinio_cognitivo_valid` | `tests/test_signatures.py` | pytest test function |
| `test_raciocinio_cognitivo_empty_field` | `tests/test_signatures.py` | pytest test function |
| `test_raciocinio_cognitivo_short_field` | `tests/test_signatures.py` | pytest test function |
| `test_validate_raciocinio_missing_labels` | `tests/test_signatures.py` | pytest test function |
| `test_validate_raciocinio_valid` | `tests/test_signatures.py` | pytest test function |
| `test_seed_mutador_cognitivo_present` | `tests/test_registry.py` | pytest test function |
| `test_seed_mutador_cognitivo_idempotent` | `tests/test_registry.py` | pytest test function |
| `test_seed_prompt_contains_premissas` | `tests/test_registry.py` | pytest test function |
| `test_prior_boost_counts` | `tests/test_bandit.py` | pytest test function |
| `test_bandit_reset_on_reinit` | `tests/test_bandit.py` | pytest test function |
| `test_cognitivo_prior_count_default` | `tests/test_config.py` | pytest test function |
| `test_cognitivo_prior_count_env_override` | `tests/test_config.py` | pytest test function |
| `test_cognitivo_prior_mean_delta_default` | `tests/test_config.py` | pytest test function |
| `test_optimizer_cognitivo_routing` | `tests/test_optimizer.py` | pytest test function (extensão) |
| `test_optimizer_non_cognitivo_routing` | `tests/test_optimizer.py` | pytest test function (extensão) |
| `test_optimizer_init_cognitivo_prior` | `tests/test_optimizer_integration.py` | pytest test function |
| `test_optimizer_agent_cognitivo_attr` | `tests/test_optimizer_integration.py` | pytest test function |

---

## Tasks

### Task 06-03-01 — Criar `tests/test_signatures.py` e `tests/test_config.py`

<read_first>
- `tests/conftest.py` — ler inteiro: fixtures disponíveis, imports e padrões de setup usados nos testes existentes.
- `tests/test_optimizer.py` — ler inteiro: padrão de import, uso de `unittest.mock`, estrutura dos tests existentes.
- `src/signatures.py` — ler inteiro: confirmar nomes exatos de campos em `MutadorCognitivoAgent` (para field inspection) e `RaciocinioCognitivo` (para validator tests).
- `src/config.py` — ler `get_mcts_config()` para confirmar nomes das chaves e env vars.
- `.planning/phases/06-mutador-cognitivo/06-RESEARCH.md` — seções "Component 1", "Component 2", "Component 6" da Validation Architecture.
</read_first>

<action>
Criar `tests/test_signatures.py` (arquivo novo) com os seguintes testes, sem dependência de LLM:

1. `test_mutador_cognitivo_agent_fields`: import `MutadorCognitivoAgent`; instanciar `dspy.ChainOfThought(MutadorCognitivoAgent)`; verificar que `MutadorCognitivoAgent.output_fields` contém as chaves `'critica'`, `'raciocinio_estruturado'`, `'nova_instrucao'`; verificar que `MutadorCognitivoAgent.input_fields` contém `'instrucao_anterior'`, `'nota_anterior'`, `'feedback_juiz'`, `'estrategia_mutacao'`.

2. `test_raciocinio_cognitivo_valid`: instanciar `RaciocinioCognitivo(premissas="a"*15, deducoes="b"*15, conclusao="c"*15)` — deve não levantar exceção.

3. `test_raciocinio_cognitivo_empty_field`: usar `pytest.raises(ValidationError)` ao instanciar `RaciocinioCognitivo(premissas="", deducoes="b"*15, conclusao="c"*15)`.

4. `test_raciocinio_cognitivo_short_field`: usar `pytest.raises(ValidationError)` ao instanciar com qualquer campo de len < 10 (ex: `premissas="abc"`).

5. `test_validate_raciocinio_missing_labels`: chamar `_validate_raciocinio("Texto sem labels")` — deve levantar `ValueError`.

6. `test_validate_raciocinio_valid`: chamar `_validate_raciocinio("**Premissas:** algo\n**Deduções:** algo\n**Conclusão:** algo")` — deve não levantar exceção.

Criar `tests/test_config.py` (arquivo novo) com:

1. `test_cognitivo_prior_count_default`: chamar `get_mcts_config()` sem env vars; assert `result['cognitivo_prior_count'] == 4`.

2. `test_cognitivo_prior_count_env_override`: usar `monkeypatch.setenv('MCTS_COGNITIVO_PRIOR_COUNT', '6')`, importar `get_mcts_config` de `src.config` (com reload via `importlib.reload` se necessário); assert `result['cognitivo_prior_count'] == 6`.

3. `test_cognitivo_prior_mean_delta_default`: assert `result['cognitivo_prior_mean_delta'] == 0.05`.

NÃO usar `unittest.mock.patch` desnecessário — os tests de config são puramente funcionais (chamada direta à função). DSPy tests de field inspection não requerem LLM configurado.
</action>

<acceptance_criteria>
- `tests/test_signatures.py` existe e contém as 6 funções de teste listadas acima.
- `tests/test_config.py` existe e contém as 3 funções de teste listadas acima.
- `pytest tests/test_signatures.py -x -q` exits 0.
- `pytest tests/test_config.py -x -q` exits 0.
- Nenhum teste em `test_signatures.py` faz chamada de LLM — verificável por ausência de `dspy.settings.configure` ou fixture de LLM real.
- `test_raciocinio_cognitivo_empty_field` e `test_raciocinio_cognitivo_short_field` levantam `ValidationError` (de pydantic) — não `AssertionError`.
</acceptance_criteria>

---

### Task 06-03-02 — Criar `tests/test_registry.py`, `tests/test_bandit.py` e estender `tests/test_optimizer.py`

<read_first>
- `tests/test_optimizer.py` — ler inteiro: testes existentes (`test_optimizer_layer1_hard_pruning`, `test_optimizer_layer2_penalty_multiplier`) — os novos testes devem ser ADICIONADOS ao final, sem remover nenhum teste existente.
- `src/mutation_strategies/registry.py` — ler inteiro: `StrategyRegistry.__init__`, `add_strategy`, `_load`, `get_all_keys`, `get_name`, `get_prompt`, para criar testes precisos.
- `src/mutation_strategies/bandit.py` — ler inteiro: `MutationBandit.__init__`, `load_priors`, `_ensure_key`, `_counts`, `_rewards`, fórmula `virtual_count = min(int(stats['count'] * 0.5), 10)`.
- `tests/conftest.py` — fixtures disponíveis.
- `.planning/phases/06-mutador-cognitivo/06-RESEARCH.md` — seções "Component 3", "Component 4", "Component 5" da Validation Architecture.
</read_first>

<action>
Criar `tests/test_registry.py` (arquivo novo) com:

1. `test_seed_mutador_cognitivo_present`: instanciar `StrategyRegistry()`; assert `'mutador_cognitivo' in registry.get_all_keys()`; assert `registry.get_name('mutador_cognitivo') == 'Mutador Cognitivo'`.

2. `test_seed_mutador_cognitivo_idempotent`: usar `unittest.mock.patch.object(StrategyRegistry, 'add_strategy')` como context manager; instanciar `StrategyRegistry()`; assert `mock_add.call_count == 0` para a key `'mutador_cognitivo'` na segunda instanciação (quando o JSON já tem a key — simular via `patch` do `_load` para retornar dict com key já presente).

3. `test_seed_prompt_contains_premissas`: instanciar `StrategyRegistry()`; `prompt = registry.get_prompt('mutador_cognitivo')`; assert `'premissas' in prompt.lower()`.

Criar `tests/test_bandit.py` (arquivo novo) com:

1. `test_prior_boost_counts`: instanciar `MutationBandit(c_param=1.0)`; chamar `load_priors({'mutador_cognitivo': {'count': 4, 'mean_delta': 0.05}})`; assert `bandit._counts['mutador_cognitivo'] == 2` (formula: `min(int(4 * 0.5), 10) = 2`); assert `abs(bandit._rewards['mutador_cognitivo'] - 0.10) < 1e-9` (2 * 0.05).

2. `test_bandit_reset_on_reinit`: injetar priors; criar nova instância de `MutationBandit(c_param=1.0)`; assert `'mutador_cognitivo' not in new_bandit._counts` (resets clean — bandit é stateless entre inits).

Estender `tests/test_optimizer.py` (adicionar ao final, não remover nada):

1. `test_optimizer_cognitivo_routing`: usar mocks para `dspy.ChainOfThought`, `SelfReflectiveAgent`, `MutadorCognitivoAgent`; criar instância de `Optimizer` com `skill_original="test"`; usar `patch.object(optimizer.mutation_bandit, 'select', return_value='mutador_cognitivo')`; chamar `optimizer._expand_node(mock_leaf)`; assert `optimizer.agent_cognitivo` foi chamado; assert `optimizer.agent` NÃO foi chamado no branch cognitivo.

2. `test_optimizer_non_cognitivo_routing`: mesma estrutura mas `select` retorna `'alguma_outra_estrategia'`; assert `optimizer.agent` foi chamado; assert `optimizer.agent_cognitivo` NÃO foi chamado.
</action>

<acceptance_criteria>
- `tests/test_registry.py` existe com as 3 funções de teste.
- `tests/test_bandit.py` existe com as 2 funções de teste.
- `tests/test_optimizer.py` contém `test_optimizer_cognitivo_routing` e `test_optimizer_non_cognitivo_routing` ao final.
- `tests/test_optimizer.py` ainda contém `test_optimizer_layer1_hard_pruning` e `test_optimizer_layer2_penalty_multiplier` (não foram removidos).
- `pytest tests/test_registry.py -x -q` exits 0.
- `pytest tests/test_bandit.py -x -q` exits 0.
- `pytest tests/test_optimizer.py -x -q` exits 0 (incluindo os novos testes de routing e os existentes).
- `test_prior_boost_counts`: `bandit._counts['mutador_cognitivo'] == 2` (não 4 — a fórmula `min(int(4*0.5), 10)` aplica 50% discount).
</acceptance_criteria>

---

### Task 06-03-03 — Criar `tests/test_optimizer_integration.py` e executar suite completa

<read_first>
- `src/optimizer.py` — linhas 70-155 (`Optimizer.__init__`): ver quais módulos externos são instanciados (ExperienceStore, ValueEstimator, MutationBandit, dspy.ChainOfThought, load_avaliador) para determinar o que deve ser mockado no smoke test.
- `tests/conftest.py` — fixtures disponíveis para reutilização.
- `tests/test_optimizer.py` — padrão de mock de `Optimizer` usado nos testes existentes.
- `.planning/phases/06-mutador-cognitivo/06-RESEARCH.md` — seção "Component 7: Integration Smoke Test" para a lista de assertions específicas.
</read_first>

<action>
Criar `tests/test_optimizer_integration.py` (arquivo novo) com:

1. Fixture de módulo (ou setup no próprio teste) que mocka: `dspy.ChainOfThought`, `dspy.Predict`, `src.optimizer.ExperienceStore`, `src.optimizer.ValueEstimator`, `src.optimizer.load_avaliador`, `src.optimizer.funcao_de_recompensa`.

2. `test_optimizer_init_cognitivo_prior`: com todos os mocks ativos, instanciar `Optimizer(skill_original="Test skill")`; assert `'mutador_cognitivo' in optimizer.mutation_bandit._counts`; assert `optimizer.mutation_bandit._counts['mutador_cognitivo'] > 0`.

3. `test_optimizer_agent_cognitivo_attr`: com todos os mocks ativos, instanciar `Optimizer(skill_original="Test skill")`; assert `hasattr(optimizer, 'agent_cognitivo')`; assert `optimizer.agent_cognitivo is not None`.

Após criar os arquivos, executar a suite completa para verificação de regressão:

```bash
pytest tests/ -x -q
```

Se algum teste falhar, diagnosticar e corrigir o problema antes de considerar o plano concluído. O comando deve retornar exit 0 com todos os testes passando.
</action>

<acceptance_criteria>
- `tests/test_optimizer_integration.py` existe com as 2 funções de teste.
- `pytest tests/test_optimizer_integration.py -x -q` exits 0.
- `pytest tests/ -x -q` exits 0 (suite completa verde).
- `pytest tests/ -v` lista pelo menos os seguintes testes como PASSED:
  - `test_signatures.py::test_mutador_cognitivo_agent_fields`
  - `test_signatures.py::test_raciocinio_cognitivo_valid`
  - `test_signatures.py::test_raciocinio_cognitivo_empty_field`
  - `test_registry.py::test_seed_mutador_cognitivo_present`
  - `test_bandit.py::test_prior_boost_counts`
  - `test_config.py::test_cognitivo_prior_count_default`
  - `test_optimizer.py::test_optimizer_cognitivo_routing`
  - `test_optimizer_integration.py::test_optimizer_init_cognitivo_prior`
- Testes existentes antes desta fase (`test_optimizer_layer1_hard_pruning`, `test_optimizer_layer2_penalty_multiplier`, `test_heuristic_evaluator.py`, `test_semantic_evaluator.py`) ainda passam (exit 0).
</acceptance_criteria>

---

## Verification

```bash
# Suite completa
pytest tests/ -v

# Por arquivo
pytest tests/test_signatures.py tests/test_config.py tests/test_registry.py tests/test_bandit.py tests/test_optimizer.py tests/test_optimizer_integration.py -v
```

## Must-Haves

- `tests/test_signatures.py`, `tests/test_registry.py`, `tests/test_bandit.py`, `tests/test_config.py`, `tests/test_optimizer_integration.py` criados como arquivos novos.
- `tests/test_optimizer.py` extendido com routing tests — testes EXISTENTES preservados.
- `pytest tests/ -x -q` exits 0 (suite completa verde).
- Nenhum teste usa LLM real — todos mockam dspy/LLM onde necessário.
- `test_prior_boost_counts` assert `_counts['mutador_cognitivo'] == 2` (não 4).
- `test_optimizer_cognitivo_routing` verifica que `agent_cognitivo` é chamado e `agent` NÃO é chamado no branch cognitivo.
- `test_optimizer_non_cognitivo_routing` verifica regressão: branch else usa `agent` (não `agent_cognitivo`).
