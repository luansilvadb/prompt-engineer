---
phase: 06-mutador-cognitivo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/signatures.py
  - src/config.py
  - src/mutation_strategies/registry.py
  - tests/test_signatures.py
  - tests/test_config.py
  - tests/test_registry.py
autonomous: true
requirements:
  - COGN-01
must_haves:
  truths:
    - "RaciocinioCognitivo Pydantic model rejects empty or too-short (<10 char) values for premissas, deducoes, conclusao"
    - "MutadorCognitivoAgent DSPy Signature exposes raciocinio_estruturado as an OutputField alongside critica and nova_instrucao"
    - "MutadorCognitivoAgent redeclares all 4 InputFields from SelfReflectiveAgent explicitly (no class inheritance)"
    - "MutadorCognitivoOutput Pydantic model rejects nova_instrucao missing any of the 3 required Markdown headings"
    - "_validate_raciocinio parses labeled sections from a raw LLM string and raises on missing labels or short content"
    - "get_mcts_config returns cognitivo_prior_count (int, default 4) and cognitivo_prior_mean_delta (float, default 0.05)"
    - "StrategyRegistry seeds 'mutador_cognitivo' key on init with name 'Mutador Cognitivo' and a non-empty prompt"
    - "Seed registration is idempotent: second instantiation does not call save() again"
    - "SelfReflectiveAgent class is not modified (D-01 constraint)"
  artifacts:
    - "src/signatures.py contains class RaciocinioCognitico(BaseModel)"
    - "src/signatures.py contains class MutadorCognitivoAgent(dspy.Signature)"
    - "src/signatures.py contains class MutadorCognitivoOutput(BaseModel)"
    - "src/signatures.py contains function _validate_raciocinio"
    - "src/config.py get_mcts_config returns dict with keys cognitivo_prior_count and cognitivo_prior_mean_delta"
    - "src/mutation_strategies/registry.py contains method _seed_hardcoded_strategies"
    - "tests/test_signatures.py exists with all tests passing"
    - "tests/test_config.py exists with all tests passing"
    - "tests/test_registry.py exists with all tests passing"
  key_links:
    - "MutadorCognitivoAgent importable from src.signatures (consumed by optimizer.py in Plan 02)"
    - "_validate_raciocinio importable from src.signatures (consumed by optimizer.py in Plan 02)"
    - "mutador_cognitivo strategy key in registry matches key expected by bandit load_priors and optimizer routing"
    - "cognitivo_prior_count and cognitivo_prior_mean_delta in config dict match keys read by optimizer.py in Plan 02"
  prohibitions:
    - statement: "SelfReflectiveAgent class must not be modified (D-01)"
      status: resolved
      verification: "git diff src/signatures.py shows no changes to SelfReflectiveAgent class definition (lines 23-30)"
    - statement: "MCTS tree logic (selection, backpropagation, reward shaping) must not be modified (REQUIREMENTS Out of Scope)"
      status: resolved
      verification: "No changes to selection(), backpropagation(), _run_mcts_iteration() in this plan"
---

<objective>
Create the foundation layer for the Mutador Cognitivo: the DSPy Signature, Pydantic validators, config knobs, and registry seed that Plan 02 wires into the MCTS optimizer.

Purpose: D-01 requires MutadorCognitivoAgent as an independent DSPy Signature with raciocinio_estruturado. D-02 requires Pydantic validation of the reasoning structure and mandatory Markdown sections in nova_instrucao. D-03 requires a hardcoded seed in StrategyRegistry. D-04 requires configurable prior boosting knobs in config.py. All four are prerequisites for the optimizer routing in Plan 02.

Output: New classes and function in src/signatures.py, two new config keys in src/config.py, a seed method in src/mutation_strategies/registry.py, and three new test files.
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mutador-cognitivo/06-CONTEXT.md
@.planning/phases/06-mutador-cognitivo/06-RESEARCH.md
@.planning/phases/06-mutador-cognitivo/06-PATTERNS.md
@.planning/phases/06-mutador-cognitivo/06-VALIDATION.md

@src/signatures.py
@src/config.py
@src/mutation_strategies/registry.py
@tests/conftest.py
@tests/test_heuristic_evaluator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — RaciocinioCognitivo, MutadorCognitivoAgent, MutadorCognitivoOutput, _validate_raciocinio in signatures.py</name>
  <files>tests/test_signatures.py, src/signatures.py</files>
  <read_first>
    - src/signatures.py (SelfReflectiveAgent fields at lines 23-30, GeracaoSkill validator at lines 5-13, Avaliacao multi-field validator at lines 32-56 — all patterns to replicate)
    - tests/conftest.py (mock_heavy_evaluators fixture for DSPy instantiation without network)
    - tests/test_heuristic_evaluator.py (plain test-function pattern with direct asserts)
    - .planning/phases/06-mutador-cognitivo/06-PATTERNS.md (DSPy Signature declaration pattern, Pydantic validator pattern)
    - .planning/phases/06-mutador-cognitivo/06-RESEARCH.md (Risk 1: redeclare fields explicitly; Risk 2: post-hoc validation; Risk 3: Markdown labels for string search)
  </read_first>
  <behavior>
    - RaciocinioCognitivo(premissas, deducoes, conclusao) with all fields >= 10 chars does not raise
    - RaciocinioCognitivo with any field empty string raises pydantic.ValidationError
    - RaciocinioCognitivo with any field < 10 chars raises pydantic.ValidationError
    - MutadorCognitivoOutput(nova_instrucao) containing all 3 Markdown headings and >= 50 chars does not raise
    - MutadorCognitivoOutput(nova_instrucao) missing any heading raises pydantic.ValidationError
    - _validate_raciocinio with a string containing all 3 labeled sections (Premissas, Ducoes, Conclusao) each with >= 10 chars of content does not raise
    - _validate_raciocinio with a string missing any label raises ValueError
    - dspy.ChainOfThought(MutadorCognitivoAgent) output_fields contains raciocinio_estruturado, critica, nova_instrucao
    - dspy.ChainOfThought(MutadorCognitivoAgent) input_fields contains instrucao_anterior, nota_anterior, feedback_juiz, estrategia_mutacao
  </behavior>
  <action>
RED phase — Create tests/test_signatures.py with these test functions (follow the plain def test_... pattern from test_heuristic_evaluator.py, no classes):
- test_raciocinio_cognitivo_happy_path: construct RaciocinioCognitivo with premissas, deducoes, conclusao each a string of >= 10 chars; assert no exception.
- test_raciocinio_cognitivo_empty_field: construct with premissas set to empty string; assert pydantic.ValidationError is raised.
- test_raciocinio_cognitivo_short_field: construct with any field < 10 chars; assert ValidationError.
- test_mutador_cognitivo_output_valid: construct MutadorCognitivoOutput with nova_instrucao containing all 3 Markdown headings (with accent: Raciocinio section, Regras section, Conclusao section) and total length >= 50 chars; assert no exception.
- test_mutador_cognitivo_output_missing_heading: construct with nova_instrucao missing one heading; assert ValidationError.
- test_validate_raciocinio_valid: call _validate_raciocinio with a string containing labeled sections (e.g. "Premissas: the feedback reveals critical gaps. Ducoes: the instruction must change approach. Conclusao: rewrite with structured logic."); assert no exception.
- test_validate_raciocinio_missing_label: call with a string missing any of the 3 labels; assert ValueError or ValidationError raised.
- test_mutador_cognitivo_agent_output_fields: instantiate dspy.ChainOfThought(MutadorCognitivoAgent) (use mock_heavy_evaluators fixture to avoid network config); assert the output_fields dict contains keys raciocinio_estruturado, critica, nova_instrucao.
- test_mutador_cognitivo_agent_input_fields: assert input_fields dict contains keys instrucao_anterior, nota_anterior, feedback_juiz, estrategia_mutacao.

Import line: from src.signatures import RaciocinioCognitivo, MutadorCognitivoAgent, MutadorCognitivoOutput, _validate_raciocinio. Also import ValidationError from pydantic. Import pytest for the fixture-based test.

Run tests — they MUST fail (classes do not exist yet).

GREEN phase — Implement in src/signatures.py, appending AFTER the SelfReflectiveAgent class (after line 30). Do NOT modify SelfReflectiveAgent — D-01 requires it to remain intact.

1. Class RaciocinioCognitivo(BaseModel) — per D-02. Three str fields: premissas, deducoes, conclusao, each with Field(description=...) in Portuguese. Add a @field_validator decorated with @classmethod targeting all three field names, checking if v is falsy or len(v.strip()) < 10, raising ValueError with a Portuguese message. Follow the exact pattern of GeracaoSkill.validar_tamanho_instrucao (lines 9-13).

2. Class MutadorCognitivoAgent(dspy.Signature) — per D-01. Docstring must carry imperative Portuguese instructions: the agent MUST fill raciocinio_estruturado with premissas, deducoes, conclusao before rewriting; nova_instrucao MUST contain the 3 Markdown headings derived from the reasoning. Redeclare ALL 4 InputFields from SelfReflectiveAgent with identical names and desc values: instrucao_anterior, nota_anterior, feedback_juiz, estrategia_mutacao. Then redeclare critica and nova_instrucao as OutputFields with updated desc strings mentioning the cognitive derivation. Then add raciocinio_estruturado as a 3rd OutputField with desc stating it must contain labeled sections for premissas, deducoes, conclusao and cannot be generic. Do NOT inherit from SelfReflectiveAgent — redeclare all fields explicitly per RESEARCH Risk 1 (DSPy field inheritance is version-dependent and unreliable).

3. Class MutadorCognitivoOutput(BaseModel) — per D-02. Single field nova_instrucao: str = Field(description=...). Add @field_validator('nova_instrucao') with @classmethod that: normalizes v via v.lower(), checks for presence of the 3 lowercased accented Markdown headings (the Raciocinio heading, the Regras heading, the Conclusao heading — all lowercased), collects any missing headings into a list, raises ValueError if any are missing listing them. Also raises ValueError if len(v.strip()) < 50. Follow the GeracaoSkill validator pattern.

4. Function _validate_raciocinio(raciocinio_str: str) -> None — per D-02 and RESEARCH Risk 2/3. This is a standalone module-level function, NOT a method on any class. It takes the raw LLM string from predicao.raciocinio_estruturado. It searches case-insensitively (via .lower() on the input) for the labels "premissas", "dedu" (to match both accented and unaccented Deducoes), and "conclus" (to match both accented and unaccented Conclusao). For each label found, extract the text between that label and the next label (or end of string), stripped. If any label is missing, raise ValueError with a Portuguese message listing which labels are missing. Construct RaciocinioCognitivo with the extracted premissas, deducoes, conclusao values — this triggers the field validators which check length >= 10. Let any ValidationError from RaciocinioCognitivo propagate to the caller. The function returns None on success.

Run tests — they MUST pass.
  </action>
  <acceptance_criteria>
    - tests/test_signatures.py exits 0: python -m pytest tests/test_signatures.py -x -q
    - src/signatures.py contains the string "class RaciocinioCognitivo(BaseModel)"
    - src/signatures.py contains the string "class MutadorCognitivoAgent(dspy.Signature)"
    - src/signatures.py contains the string "class MutadorCognitivoOutput(BaseModel)"
    - src/signatures.py contains the string "def _validate_raciocinio"
    - src/signatures.py contains the string "raciocinio_estruturado" (the OutputField)
    - SelfReflectiveAgent class definition (lines 23-30) is unchanged: git diff src/signatures.py shows no modifications to the SelfReflectiveAgent class block
    - dspy.ChainOfThought(MutadorCognitivoAgent) instantiated without error in test
    - RaciocinioCognitivo with empty field raises ValidationError (test passes)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_signatures.py -x -q</automated>
  </verify>
  <done>All tests in test_signatures.py pass. Four new symbols exist in signatures.py: RaciocinioCognitivo, MutadorCognitivoAgent, MutadorCognitivoOutput, _validate_raciocinio. SelfReflectiveAgent is untouched (D-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — cognitivo config keys in config.py + hardcoded seed in registry.py</name>
  <files>tests/test_config.py, tests/test_registry.py, src/config.py, src/mutation_strategies/registry.py</files>
  <read_first>
    - src/config.py (get_mcts_config function at lines 62-99 — the os.environ.get + int()/float() cast pattern to replicate)
    - src/mutation_strategies/registry.py (StrategyRegistry.__init__ at lines 26-28, add_strategy at lines 53-55, _load at lines 34-41, save at lines 43-51)
    - tests/conftest.py (mock_heavy_evaluators fixture)
    - tests/test_optimizer.py (fixture-based test pattern with MagicMock)
    - .planning/phases/06-mutador-cognitivo/06-PATTERNS.md (Config Knob Convention, Seed registration pattern)
    - .planning/phases/06-mutador-cognitivo/06-RESEARCH.md (Section 2: registry seed details, Section 5: config key details)
  </read_first>
  <behavior>
    - get_mcts_config() with MCTS_COGNITIVO_PRIOR_COUNT=6 returns cognitivo_prior_count == 6
    - get_mcts_config() with MCTS_COGNITIVO_PRIOR_MEAN_DELTA=0.1 returns cognitivo_prior_mean_delta == 0.1
    - get_mcts_config() without env vars returns cognitivo_prior_count == 4 and cognitivo_prior_mean_delta == 0.05
    - StrategyRegistry() has 'mutador_cognitivo' in get_all_keys()
    - registry.get_prompt('mutador_cognitivo') returns non-empty string containing "premissas"
    - registry.get_name('mutador_cognitivo') returns 'Mutador Cognitivo'
    - Idempotency: second StrategyRegistry() instantiation with key already present does not call save()
  </behavior>
  <action>
RED phase — Create two test files:

tests/test_config.py (follow test_heuristic_evaluator.py pattern — plain functions, use monkeypatch for env isolation):
- test_cognitivo_config_defaults: delete env vars MCTS_COGNITIVO_PRIOR_COUNT and MCTS_COGNITIVO_PRIOR_MEAN_DELTA via monkeypatch.delenv (raising=False); call get_mcts_config(); assert cfg['cognitivo_prior_count'] == 4 and cfg['cognitivo_prior_mean_delta'] == 0.05.
- test_cognitivo_config_override: set MCTS_COGNITIVO_PRIOR_COUNT=6 and MCTS_COGNITIVO_PRIOR_MEAN_DELTA=0.1 via monkeypatch.setenv; call get_mcts_config(); assert cfg['cognitivo_prior_count'] == 6 and cfg['cognitivo_prior_mean_delta'] == 0.1.
Import: from src.config import get_mcts_config.

tests/test_registry.py (follow test_optimizer.py pattern — use mock_heavy_evaluators fixture if needed, MagicMock for save):
- test_seed_registered: instantiate StrategyRegistry(); assert 'mutador_cognitivo' in registry.get_all_keys().
- test_seed_prompt_content: assert registry.get_prompt('mutador_cognitivo') is non-empty and contains the substring "premissas" (case-insensitive).
- test_seed_name: assert registry.get_name('mutador_cognitivo') == 'Mutador Cognitivo'.
- test_seed_idempotent: instantiate StrategyRegistry() twice; on the second instantiation, patch save with MagicMock and assert save was NOT called (key already present from first instantiation or persisted JSON).
Import: from src.mutation_strategies.registry import StrategyRegistry. Use from unittest.mock import patch, MagicMock.

Run tests — they MUST fail (config keys and seed method do not exist yet).

GREEN phase — Implement in two source files:

In src/config.py, append two new keys to the dict returned by get_mcts_config() (after the existing verbosity_penalty_factor key at line 98, before the closing brace). Follow the exact pattern of existing keys — inline comment above each, os.environ.get with MCTS_ prefix, string default, explicit int() or float() cast. Per D-04:
- 'cognitivo_prior_count': int(os.environ.get('MCTS_COGNITIVO_PRIOR_COUNT', '4')) — with inline comment explaining it controls virtual pull count for MutadorCognitivo prior boosting.
- 'cognitivo_prior_mean_delta': float(os.environ.get('MCTS_COGNITIVO_PRIOR_MEAN_DELTA', '0.05')) — with inline comment explaining it controls the virtual mean reward delta.

In src/mutation_strategies/registry.py, per D-03:
- Add a call to self._seed_hardcoded_strategies() as the LAST line of __init__ (after self._load() on line 28).
- Add the _seed_hardcoded_strategies(self) method to the StrategyRegistry class. The method defines a local constant COGNITIVO_KEY = 'mutador_cognitivo'. It checks if COGNITIVO_KEY is not in self.strategies (idempotency guard — registry persists to JSON on save(), so on restart the key already exists per RESEARCH Risk 4). If the key is absent, it calls self.add_strategy with key='mutador_cognitivo', name='Mutador Cognitivo', and a prompt string that instructs the agent to apply mandatory structured logical derivation with three steps: (1) Premissas — what the feedback reveals about the current instruction; (2) Deducoes — logical implications about what needs to change; (3) Conclusao — the architectural rule the new instruction must implement. The prompt must also state that the new instruction MUST contain the 3 Markdown section headings derived from this reasoning. The prompt must contain the word "premissas" (lowercase) so the test assertion passes.

Run tests — they MUST pass.
  </action>
  <acceptance_criteria>
    - tests/test_config.py exits 0: python -m pytest tests/test_config.py -x -q
    - tests/test_registry.py exits 0: python -m pytest tests/test_registry.py -x -q
    - src/config.py contains the string "cognitivo_prior_count"
    - src/config.py contains the string "cognitivo_prior_mean_delta"
    - src/config.py contains the string "MCTS_COGNITIVO_PRIOR_COUNT"
    - src/config.py contains the string "MCTS_COGNITIVO_PRIOR_MEAN_DELTA"
    - src/mutation_strategies/registry.py contains the string "_seed_hardcoded_strategies"
    - src/mutation_strategies/registry.py contains the string "mutador_cognitivo"
    - StrategyRegistry().get_name('mutador_cognitivo') returns 'Mutador Cognitivo'
    - get_mcts_config()['cognitivo_prior_count'] == 4 with no env vars set
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_config.py tests/test_registry.py -x -q</automated>
  </verify>
  <done>Config has two new cognitivo keys with correct defaults and env-var overrides. Registry seeds mutador_cognitivo on init with correct name and prompt. Seed is idempotent. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output to Python | DSPy prediction produces raciocinio_estruturado and nova_instrucao as free-form strings crossing into Python objects |
| Environment to config | Environment variables MCTS_COGNITIVO_PRIOR_COUNT and MCTS_COGNITIVO_PRIOR_MEAN_DELTA read via os.environ.get |
| JSON file to Python | discovered_strategies.json read by StrategyRegistry._load (seed is hardcoded, not from JSON) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | RaciocinioCognitivo / _validate_raciocinio | medium | mitigate | Pydantic validator checks all 3 fields non-empty and >= 10 chars; _validate_raciocinio raises ValueError on missing labels; soft enforcement in optimizer (Plan 02) logs via on_error without crashing |
| T-06-02 | Tampering | MutadorCognitivoOutput | low | mitigate | Validator checks for 3 Markdown headings with .lower() normalization; soft enforcement (warning only per RESEARCH Risk 6) |
| T-06-03 | Tampering | config.py get_mcts_config | low | mitigate | int() and float() casts on env var values raise ValueError on non-numeric input, preventing injection of malformed values |
| T-06-04 | Tampering | registry.py _seed_hardcoded_strategies | low | mitigate | Idempotency guard (if key not in self.strategies) prevents duplicate registration; seed prompt is hardcoded in Python source, not read from external JSON |
| T-06-SC | Tampering | No new pip packages installed | low | accept | All dependencies (dspy, pydantic) already in project; no supply chain risk introduced by this phase |
</threat_model>

<verification>
- All three test files pass: python -m pytest tests/test_signatures.py tests/test_config.py tests/test_registry.py -x -q
- Existing tests still pass (no regression): python -m pytest tests/test_optimizer.py tests/test_heuristic_evaluator.py tests/test_semantic_evaluator.py -x -q
- SelfReflectiveAgent unchanged: git diff src/signatures.py shows no modifications to lines 23-30
- Full suite green: python -m pytest tests/ -x -q
</verification>

<success_criteria>
- MutadorCognitivoAgent DSPy Signature exists with raciocinio_estruturado OutputField (D-01)
- RaciocinioCognitivo Pydantic model validates premissas/deducoes/conclusao are non-empty and >= 10 chars (D-02)
- MutadorCognitivoOutput validates nova_instrucao contains 3 required Markdown headings (D-02)
- _validate_raciocinio post-hoc validator parses labeled sections and raises on invalid input (D-02)
- config.py exposes cognitivo_prior_count and cognitivo_prior_mean_delta with env-var overrides (D-04)
- StrategyRegistry seeds mutador_cognitivo on init, idempotently (D-03)
- SelfReflectiveAgent is not modified (D-01)
- All existing tests pass (no regression)
</success_criteria>

<output>
Create `.planning/phases/06-mutador-cognitivo/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New classes in src/signatures.py
- `RaciocinioCognitivo(BaseModel)` — Pydantic model with fields `premissas`, `deducoes`, `conclusao` (all str), validated by `validar_campo_preenchido` field_validator
- `MutadorCognitivoAgent(dspy.Signature)` — DSPy Signature with 4 InputFields (instrucao_anterior, nota_anterior, feedback_juiz, estrategia_mutacao) and 3 OutputFields (critica, raciocinio_estruturado, nova_instrucao)
- `MutadorCognitivoOutput(BaseModel)` — Pydantic model with field `nova_instrucao` (str), validated by `validar_secoes_cognitivas` field_validator checking for 3 Markdown headings

### New function in src/signatures.py
- `_validate_raciocinio(raciocinio_str: str) -> None` — standalone post-hoc validator parsing labeled sections from LLM output

### New config keys in src/config.py (in get_mcts_config dict)
- `cognitivo_prior_count` (int, default 4, env var MCTS_COGNITIVO_PRIOR_COUNT)
- `cognitivo_prior_mean_delta` (float, default 0.05, env var MCTS_COGNITIVO_PRIOR_MEAN_DELTA)

### New method in src/mutation_strategies/registry.py
- `StrategyRegistry._seed_hardcoded_strategies(self)` — registers the `mutador_cognitivo` strategy seed

### New strategy key
- `mutador_cognitivo` — snake_case key registered in StrategyRegistry, consumed by MutationBandit and Optimizer

### New test files
- `tests/test_signatures.py` — unit tests for RaciocinioCognitivo, MutadorCognitivoAgent, MutadorCognitivoOutput, _validate_raciocinio
- `tests/test_config.py` — unit tests for cognitivo config keys
- `tests/test_registry.py` — unit tests for seed registration and idempotency
