---
phase: 06-mutador-cognitivo
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/optimizer.py
  - tests/test_bandit.py
  - tests/test_optimizer_integration.py
  - tests/test_optimizer.py
autonomous: true
requirements:
  - COGN-01
must_haves:
  truths:
    - "Optimizer.__init__ instantiates self.agent_cognitivo = dspy.ChainOfThought(MutadorCognitivoAgent)"
    - "Optimizer.__init__ injects cognitivo prior boost via load_priors with config values, unconditionally (not gated by experience store)"
    - "MutationBandit.load_priors with count=4 and mean_delta=0.05 produces _counts['mutador_cognitivo'] == 2 and _rewards['mutador_cognitivo'] == 0.10"
    - "_expand_node routes to self.agent_cognitivo when bandit selects 'mutador_cognitivo', and to self.agent for all other strategies"
    - "Post-LLM validation calls _validate_raciocinio on predicao.raciocinio_estruturado with soft enforcement (logs via on_error, does not crash)"
    - "Post-LLM validation constructs MutadorCognitivoOutput with soft enforcement (logs via on_error, does not crash)"
    - "Child MCTSNode created with mutation_strategy == 'mutador_cognitivo' when cognitivo agent is used"
    - "Existing tests test_optimizer_layer1_hard_pruning and test_optimizer_layer2_penalty_multiplier pass unchanged"
  artifacts:
    - "src/optimizer.py imports MutadorCognitivoAgent and _validate_raciocinio from src.signatures"
    - "src/optimizer.py Optimizer.__init__ contains self.agent_cognitivo = dspy.ChainOfThought(MutadorCognitivoAgent)"
    - "src/optimizer.py Optimizer.__init__ contains unconditional cognitivo prior boost via load_priors"
    - "src/optimizer.py _expand_node contains strategy == 'mutador_cognitivo' branch calling self.agent_cognitivo"
    - "tests/test_bandit.py exists with all tests passing"
    - "tests/test_optimizer_integration.py exists with all tests passing"
    - "tests/test_optimizer.py contains new cognitivo routing regression test"
  key_links:
    - "Import of MutadorCognitivoAgent and _validate_raciocinio from src.signatures (created in Plan 01)"
    - "Import of MutadorCognitivoOutput from src.signatures (created in Plan 01)"
    - "config.get('cognitivo_prior_count') and config.get('cognitivo_prior_mean_delta') reading keys created in Plan 01"
    - "Strategy key 'mutador_cognitivo' matching registry seed from Plan 01 and bandit arm key"
  prohibitions:
    - statement: "MCTS tree logic (selection, backpropagation, reward shaping) must not be modified (REQUIREMENTS Out of Scope)"
      status: resolved
      verification: "git diff src/optimizer.py shows no changes to selection(), backpropagation(), _run_mcts_iteration() methods — only __init__ and _expand_node are modified"
    - statement: "API endpoint contracts must not be broken (REQUIREMENTS Out of Scope)"
      status: resolved
      verification: "No changes to any API route files or endpoint signatures"
---

<objective>
Wire the Mutador Cognitivo into the MCTS optimizer: instantiate the cognitivo agent, inject prior boosting, and route strategy selection to MutadorCognitivoAgent when the bandit selects mutador_cognitivo.

Purpose: D-03 requires optimizer.py to detect when the bandit selects MutadorCognitivo and route to MutadorCognitivoAgent instead of SelfReflectiveAgent. D-04 requires prior boosting via load_priors with configurable virtual counts. This plan depends on Plan 01 for the DSPy Signature, Pydantic validators, config keys, and registry seed.

Output: Modified optimizer.py with agent_cognitivo instantiation, prior boost, and routing intercept. Three test files covering bandit priors, integration smoke test, and regression.
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mutador-cognitivo/06-CONTEXT.md
@.planning/phases/06-mutador-cognitivo/06-RESEARCH.md
@.planning/phases/06-mutador-cognitivo/06-PATTERNS.md
@.planning/phases/06-mutador-cognitivo/06-VALIDATION.md
@.planning/phases/06-mutador-cognitivo/06-01-SUMMARY.md

@src/optimizer.py
@src/signatures.py
@src/config.py
@src/mutation_strategies/bandit.py
@src/mutation_strategies/registry.py
@tests/conftest.py
@tests/test_optimizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Bandit prior boosting test + Optimizer.__init__ agent_cognitivo and prior boost</name>
  <files>tests/test_bandit.py, src/optimizer.py</files>
  <read_first>
    - src/optimizer.py (Optimizer.__init__ lines 109-152 — agent instantiation at line 139, prior loading at lines 145-149, config fetch at line 127)
    - src/mutation_strategies/bandit.py (load_priors at lines 44-49 — formula virtual_count = min(int(count * 0.5), 10); _ensure_key at lines 39-42; select at lines 74-84; __init__ at lines 31-37)
    - src/signatures.py (MutadorCognitivoAgent class — created by Plan 01)
    - src/config.py (cognitivo_prior_count and cognitivo_prior_mean_delta keys — created by Plan 01)
    - tests/conftest.py (mock_heavy_evaluators fixture)
    - tests/test_optimizer.py (existing test pattern with mock_heavy_evaluators fixture)
    - .planning/phases/06-mutador-cognitivo/06-RESEARCH.md (Section 3: bandit prior boosting details, Section 4: optimizer routing, Risk 5: prior boosting is safe per-session)
    - .planning/phases/06-mutador-cognitivo/06-PATTERNS.md (Prior-boosting pattern, Agent instantiation pattern)
  </read_first>
  <behavior>
    - MutationBandit().load_priors({'mutador_cognitivo': {'count': 4, 'mean_delta': 0.05}}) sets _counts['mutador_cognitivo'] to 2 and _rewards['mutador_cognitivo'] to 0.10
    - Fresh MutationBandit() has _counts['mutador_cognitivo'] == 0 before load_priors (if registry seeded from Plan 01)
    - Optimizer(skill_original="Test") has attribute agent_cognitivo that is a dspy.ChainOfThought instance
    - Optimizer(skill_original="Test") has 'mutador_cognitivo' in mutation_bandit._counts with value > 0
    - Optimizer(skill_original="Test") has mutation_bandit._rewards['mutador_cognitivo'] > 0
  </behavior>
  <action>
RED phase — Create tests/test_bandit.py with these test functions (plain def test_... pattern, use mock_heavy_evaluators fixture for tests that instantiate Optimizer):

- test_bandit_cognitivo_prior_counts: instantiate MutationBandit(); call bandit.load_priors({'mutador_cognitivo': {'count': 4, 'mean_delta': 0.05}}); assert bandit._counts['mutador_cognitivo'] == 2; assert bandit._rewards['mutador_cognitivo'] == 0.10. Import: from src.mutation_strategies.bandit import MutationBandit.
- test_bandit_cognitivo_prior_formula: call load_priors with count=10, mean_delta=0.2; assert _counts['mutador_cognitivo'] == min(int(10 * 0.5), 10) == 5; assert _rewards['mutador_cognitivo'] == 0.2 * 5 == 1.0.
- test_bandit_cognitivo_key_exists: instantiate MutationBandit() (registry seeded from Plan 01); assert 'mutador_cognitivo' in bandit._counts (key added by __init__ reading registry.get_all_keys).
- test_optimizer_has_agent_cognitivo: use mock_heavy_evaluators fixture; instantiate Optimizer(skill_original="Test"); assert hasattr(opt, 'agent_cognitivo'); assert opt.agent_cognitivo is not None.
- test_optimizer_cognitivo_prior_injected: use mock_heavy_evaluators fixture; instantiate Optimizer(skill_original="Test"); assert 'mutador_cognitivo' in opt.mutation_bandit._counts; assert opt.mutation_bandit._counts['mutador_cognitivo'] > 0; assert opt.mutation_bandit._rewards['mutador_cognitivo'] > 0.

Run tests — they MUST fail (agent_cognitivo and prior boost do not exist in optimizer.py yet; bandit tests should pass if Plan 01 registry seed is active, but the optimizer tests will fail).

GREEN phase — Implement in src/optimizer.py:

1. Update the import from src.signatures at line 24. The current import is: from src.signatures import SelfReflectiveAgent, StrategyDiscoverer, funcao_de_recompensa, calcular_delta_reward, load_avaliador. Add MutadorCognitivoAgent, MutadorCognitivoOutput, and _validate_raciocinio to this import list. Per D-01 and D-03.

2. In Optimizer.__init__, after line 139 (self.agent = dspy.ChainOfThought(SelfReflectiveAgent)), add: self.agent_cognitivo = dspy.ChainOfThought(MutadorCognitivoAgent). This mirrors the existing agent instantiation pattern exactly. Per D-03.

3. In Optimizer.__init__, after the existing experience store prior loading block (lines 146-149, which is gated by if strategy_stats:), add an UNCONDITIONAL cognitivo prior boost. This is NOT inside the if strategy_stats: block — it runs regardless of whether the experience store has data. Per D-04:
   - Construct a dict: cognitivo_prior = {'mutador_cognitivo': {'count': config.get('cognitivo_prior_count', 4), 'mean_delta': config.get('cognitivo_prior_mean_delta', 0.05)}}. The config variable is already fetched at line 127 (config = get_mcts_config()).
   - Call self.mutation_bandit.load_priors(cognitivo_prior).
   - Add an on_progress log line: self.on_progress(f'[*] Mutador Cognitivo prior boosting: {cognitivo_prior["mutador_cognitivo"]["count"]} virtual count, delta={cognitivo_prior["mutador_cognitivo"]["mean_delta"]}').

Run tests — they MUST pass.
  </action>
  <acceptance_criteria>
    - tests/test_bandit.py exits 0: python -m pytest tests/test_bandit.py -x -q
    - src/optimizer.py contains the string "MutadorCognitivoAgent" in the import from src.signatures
    - src/optimizer.py contains the string "agent_cognitivo = dspy.ChainOfThought(MutadorCognitivoAgent)"
    - src/optimizer.py contains the string "cognitivo_prior" (the prior boost dict)
    - src/optimizer.py contains the string "load_priors(cognitivo_prior)" (the unconditional prior boost call)
    - hasattr(Optimizer(skill_original="Test"), 'agent_cognitivo') is True
    - Optimizer(skill_original="Test").mutation_bandit._counts['mutador_cognitivo'] > 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_bandit.py -x -q</automated>
  </verify>
  <done>Bandit prior boosting tests pass. Optimizer.__init__ instantiates agent_cognitivo and injects cognitivo prior boost unconditionally. Virtual count formula produces correct values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — Routing intercept in _expand_node + integration smoke test + regression test</name>
  <files>tests/test_optimizer_integration.py, tests/test_optimizer.py, src/optimizer.py</files>
  <read_first>
    - src/optimizer.py (_expand_node method lines 232-331 — strategy selection at line 234, __DISCOVER__ branch lines 236-260, strategy_prompt/strategy_desc at lines 262-263, retry loop lines 277-319, agent call lines 291-297, candidata extraction line 298, MCTSNode construction lines 321-328)
    - src/signatures.py (MutadorCognitivoAgent, _validate_raciocinio, MutadorCognitivoOutput — created by Plan 01)
    - tests/test_optimizer.py (existing tests test_optimizer_layer1_hard_pruning and test_optimizer_layer2_penalty_multiplier — pattern to follow for new test)
    - tests/conftest.py (mock_heavy_evaluators fixture)
    - .planning/phases/06-mutador-cognitivo/06-PATTERNS.md (Routing intercept pattern — the exact branch structure to replicate)
    - .planning/phases/06-mutador-cognitivo/06-RESEARCH.md (Section 4: optimizer routing details, Risk 6: soft enforcement for section validator)
  </read_first>
  <behavior>
    - When mutation_bandit.select() returns 'mutador_cognitivo', _expand_node calls self.agent_cognitivo (not self.agent)
    - When mutation_bandit.select() returns a non-cognitivo key, _expand_node calls self.agent (not self.agent_cognitivo)
    - After cognitivo agent call, _validate_raciocinio is called on predicao.raciocinio_estruturado; if it raises, self.on_error is called (soft enforcement, does not crash or abort retry loop)
    - After cognitivo agent call, MutadorCognitivoOutput is constructed with predicao.nova_instrucao; if it raises, self.on_error is called (soft enforcement)
    - Child MCTSNode created with mutation_strategy == 'mutador_cognitivo' when cognitivo path is taken
    - Existing test_optimizer_layer1_hard_pruning passes unchanged
    - Existing test_optimizer_layer2_penalty_multiplier passes unchanged
  </behavior>
  <action>
RED phase — Create tests/test_optimizer_integration.py and extend tests/test_optimizer.py:

tests/test_optimizer_integration.py (follow test_optimizer.py pattern — mock_heavy_evaluators fixture, MagicMock):
- test_cognitivo_routing_uses_agent_cognitivo: use mock_heavy_evaluators fixture; instantiate Optimizer(skill_original="Test"); patch opt.mutation_bandit.select to return 'mutador_cognitivo' via MagicMock; mock opt.agent_cognitivo to return a MagicMock with attributes nova_instrucao (a string >= 50 chars containing the 3 Markdown headings), critica (non-empty string), raciocinio_estruturado (a string containing the 3 labeled sections each >= 10 chars); mock opt.simulation to return (0.5, "feedback"); set opt.semantic_sim_threshold = 1.0; create a root MCTSNode(instruction="Test", last_reward=0.0); call opt._expand_node(root); assert opt.agent_cognitio was called; assert opt.agent was NOT called (use MagicMock assertion); assert the returned child has mutation_strategy == 'mutador_cognitivo'.
- test_non_cognitivo_routing_uses_agent: use mock_heavy_evaluators fixture; instantiate Optimizer; patch select to return a non-cognitivo key (e.g. 'simplificar'); mock opt.agent to return MagicMock with nova_instrucao and critica; mock opt.agent_cognitivo; call _expand_node; assert opt.agent was called; assert opt.agent_cognitivo was NOT called.
- test_cognitivo_routing_soft_validation: use mock_heavy_evaluators; instantiate Optimizer; patch select to return 'mutador_cognitivo'; mock agent_cognitivo to return MagicMock with raciocinio_estruturado set to "invalid" (missing labels) and nova_instrucao set to "short" (missing headings); mock on_error; call _expand_node; assert on_error was called at least once (soft enforcement); assert the function does NOT raise (soft enforcement does not crash).
- test_cognitivo_integration_child_strategy: use mock_heavy_evaluators; instantiate Optimizer; patch select to return 'mutador_cognitivo'; mock agent_cognitio with valid outputs; mock simulation; set semantic_sim_threshold = 1.0; create root; call _run_mcts_iteration(root); assert a child was created with mutation_strategy == 'mutador_cognitivo'.

Extend tests/test_optimizer.py — add one new test function at the end of the file:
- test_optimizer_cognitivo_regression: use mock_heavy_evaluators fixture; instantiate Optimizer(skill_original="foo"); assert hasattr(opt, 'agent_cognitivo'); assert 'mutador_cognitivo' in opt.mutation_bandit._counts. This is a lightweight regression guard ensuring Plan 01 artifacts are wired.

Run tests — they MUST fail (routing intercept does not exist yet).

GREEN phase — Implement in src/optimizer.py, modifying the _expand_node method ONLY. Do NOT modify selection(), backpropagation(), _run_mcts_iteration(), or any other method.

The current agent call block is at lines 291-297 inside the retry loop (for tentativa in range(3)). The call is:
  predicao = self.agent(instrucao_anterior=leaf.instruction, nota_anterior=nota, feedback_juiz=feedback_completo, estrategia_mutacao=strategy_prompt)
  candidata = predicao.nova_instrucao

Replace this single call with a strategy-based branch. Define a local constant COGNITIVO_KEY = 'mutador_cognitivo' at the top of the branch. Per D-03:

For the cognitivo branch (strategy == COGNITIVO_KEY):
  - Call self.agent_cognitivo with the same 4 kwargs: instrucao_anterior=leaf.instruction, nota_anterior=nota, feedback_juiz=feedback_completo, estrategia_mutacao=strategy_prompt.
  - After the call, perform two soft-enforcement validations in separate try/except blocks:
    - First: call _validate_raciocinio(predicao.raciocinio_estruturado) inside try/except Exception; on exception, call self.on_error(f'[!] raciocinio_estruturado invalido: {e}'). This does NOT abort the retry loop — it logs and continues (soft enforcement per RESEARCH Risk 6).
    - Second: construct MutadorCognitivoOutput(nova_instrucao=predicao.nova_instrucao) inside try/except Exception; on exception, call self.on_error(f'[!] nova_instrucao secoes cognitivas invalidas: {e}'). Also soft enforcement.
  - Set critica = predicao.critica and candidata = predicao.nova_instrucao (same extraction as non-cognitivo path).

For the non-cognitivo branch (else):
  - Call self.agent with the same 4 kwargs as before (identical to the current code).
  - Set critica = predicao.critica and candidata = predicao.nova_instrucao.

After the branch, the existing downstream logic (candidata validation, _should_prune check, on_progress log, break/continue) works identically for both branches since both agents expose nova_instrucao and critica. The MCTSNode construction at lines 321-328 uses mutation_strategy=strategy, which is 'mutador_cognitivo' for the cognitivo branch — no change needed there.

The __DISCOVER__ branch (lines 236-260) resolves strategy to a new UUID-suffixed key BEFORE the agent call, so it never matches 'mutador_cognitivo' — no conflict.

Run tests — they MUST pass.
  </action>
  <acceptance_criteria>
    - tests/test_optimizer_integration.py exits 0: python -m pytest tests/test_optimizer_integration.py -x -q
    - tests/test_optimizer.py exits 0: python -m pytest tests/test_optimizer.py -x -q
    - src/optimizer.py _expand_node contains the string "agent_cognitivo" (the cognitivo branch)
    - src/optimizer.py _expand_node contains the string "_validate_raciocinio" (the post-LLM validation call)
    - src/optimizer.py _expand_node contains the string "MutadorCognitivoOutput" (the nova_instrucao validation)
    - src/optimizer.py _expand_node contains the string "mutador_cognitivo" (the strategy key check)
    - Existing test test_optimizer_layer1_hard_pruning passes: python -m pytest tests/test_optimizer.py::test_optimizer_layer1_hard_pruning -x -q
    - Existing test test_optimizer_layer2_penalty_multiplier passes: python -m pytest tests/test_optimizer.py::test_optimizer_layer2_penalty_multiplier -x -q
    - When select returns 'mutador_cognitivo', agent_cognitivo is called and agent is NOT called (verified by MagicMock assertion in integration test)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_optimizer_integration.py tests/test_optimizer.py -x -q</automated>
  </verify>
  <done>Routing intercept branches on strategy == 'mutador_cognitivo' to call agent_cognitivo. Post-LLM validation uses soft enforcement (logs, does not crash). Integration smoke test proves cognitivo routing creates child with correct mutation_strategy. Existing tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output to Python | DSPy prediction from MutadorCognitivoAgent produces raciocinio_estruturado and nova_instrucao as free-form strings crossing into Python objects in _expand_node |
| Bandit selection to agent routing | Strategy key from MutationBandit.select() determines which DSPy agent is invoked — incorrect routing could bypass validation |
| Config values to bandit priors | cognitivo_prior_count and cognitivo_prior_mean_delta from config dict injected as virtual counts into bandit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | _expand_node cognitivo branch | medium | mitigate | Post-LLM validation: _validate_raciocinio checks raciocinio_estruturado for required labeled sections; MutadorCognitivoOutput checks nova_instrucao for 3 Markdown headings; both use soft enforcement (on_error log, no crash) per RESEARCH Risk 6 |
| T-06-06 | Spoofing | _expand_node strategy routing | low | mitigate | Strategy key comparison uses exact string match against 'mutador_cognitivo' constant; __DISCOVER__ branch resolves to UUID-suffixed key before agent call, never matching the cognitivo key |
| T-06-07 | Tampering | Optimizer.__init__ prior boost | low | mitigate | Prior boost is unconditional (not gated by experience store), uses config.get with defaults (4, 0.05); bandit resets clean on each Optimizer init per RESEARCH Risk 5 (no cross-session contamination) |
| T-06-08 | Denial of Service | _expand_node retry loop with cognitivo | low | accept | Cognitivo branch uses the same 3-attempt retry loop as non-cognitivo; soft validation does not add retry attempts; fallback to minimal variation on exhaustion is unchanged |
| T-06-SC | Tampering | No new pip packages installed | low | accept | All dependencies (dspy, pydantic) already in project; no supply chain risk introduced |
</threat_model>

<verification>
- All new tests pass: python -m pytest tests/test_bandit.py tests/test_optimizer_integration.py tests/test_optimizer.py -x -q
- Full suite green (no regression): python -m pytest tests/ -x -q
- Routing intercept only modifies _expand_node and __init__: git diff src/optimizer.py shows no changes to selection(), backpropagation(), simulation(), _should_prune(), notify_node(), optimize()
- Manual-only verification (requires live LLM): run python -m src.optimizer with a test skill; log nova_instrucao from a mutador_cognitivo expansion; visually confirm it contains the 3 Markdown headings (Raciocinio, Regras, Conclusao)
</verification>

<success_criteria>
- Optimizer.__init__ instantiates self.agent_cognitivo = dspy.ChainOfThought(MutadorCognitivoAgent) (D-03)
- Optimizer.__init__ injects cognitivo prior boost unconditionally via load_priors with config values (D-04)
- _expand_node routes to self.agent_cognitivo when bandit selects 'mutador_cognitivo' (D-03)
- Post-LLM validation calls _validate_raciocinio and constructs MutadorCognitivoOutput with soft enforcement (D-02)
- Child MCTSNode created with mutation_strategy == 'mutador_cognitivo' (COGN-01)
- Existing tests pass unchanged (no regression to MCTS tree logic)
- Full test suite is green
</success_criteria>

<output>
Create `.planning/phases/06-mutador-cognitivo/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Modified attributes in src/optimizer.py
- `self.agent_cognitivo` — new Optimizer instance attribute, a dspy.ChainOfThought(MutadorCognitivoAgent) instance
- Cognitivo prior boost call — unconditional `self.mutation_bandit.load_priors(cognitivo_prior)` in `__init__`
- Strategy routing branch in `_expand_node` — `if strategy == 'mutador_cognitivo'` calling `self.agent_cognitivo` instead of `self.agent`

### New imports in src/optimizer.py
- `MutadorCognitivoAgent` from `src.signatures`
- `MutadorCognitivoOutput` from `src.signatures`
- `_validate_raciocinio` from `src.signatures`

### New test files
- `tests/test_bandit.py` — unit tests for MutationBandit.load_priors with cognitivo data + Optimizer agent_cognitivo and prior boost assertions
- `tests/test_optimizer_integration.py` — integration smoke tests for cognitivo routing, non-cognitivo routing, soft validation enforcement, and child node mutation_strategy

### Extended test file
- `tests/test_optimizer.py` — new `test_optimizer_cognitivo_regression` test appended
