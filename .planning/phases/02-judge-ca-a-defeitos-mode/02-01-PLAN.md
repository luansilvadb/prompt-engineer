---
phase: 02-judge-ca-a-defeitos-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - signatures.py
  - teleprompter.py
autonomous: true
requirements: [JUD-01]

must_haves:
  truths:
    - O AvaliadorModoB existe isoladamente e exige identificação de defeitos antes das notas.
    - O modelo de dados AvaliacaoModoB processa e expõe os defeitos_encontrados para integração.
    - O sistema pode carregar modelos salvos discriminando por modo 'a' ou 'b'.
  artifacts:
    - signatures.py (com AvaliadorModoB e AvaliacaoModoB definidos)
  key_links:
    - _invoke_judge_modo_b_with converte a saída de AvaliadorModoB para AvaliacaoModoB corretamente.
---

<objective>
Criar a base estrutural para o Modo B ("Caça-Defeitos") no sistema de assinaturas DSPy, garantindo isolamento total do Modo A (fallback/debug).

Purpose: Forçar o juiz a identificar contradições e ambiguidades comportamentais ANTES de avaliar as métricas estéticas.
Output: Classes AvaliadorModoB e AvaliacaoModoB criadas; lógica de carregamento segregada por modo.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@signatures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renomear arquivo do modelo otimizado e atualizar teleprompter (D-09)</name>
  <files>outputs/models/avaliador_otimizado.json, teleprompter.py</files>
  <action>
  1. Mova o arquivo `outputs/models/avaliador_otimizado.json` para `outputs/models/avaliador_modo_a_otimizado.json` (usando `mv` ou `os.rename`). Se o arquivo ou diretório não existir (por ainda não ter havido treinamento no ambiente), você pode ignorar o erro silenciosamente. Esta renomeação prepara o terreno para a segregação (D-09).
  2. Atualize `teleprompter.py` (caso exista e referencie os caminhos antigos) para usar a nova nomenclatura do Modo A. Encontre as definições de path (como `out_path`, `candidate_path` e `bak_path`) e altere `avaliador_otimizado.json` para `avaliador_modo_a_otimizado.json`.
  </action>
  <verify>
    <automated>python -c "import os; assert not os.path.exists('outputs/models/avaliador_otimizado.json'); content = open('teleprompter.py').read() if os.path.exists('teleprompter.py') else ''; assert 'avaliador_modo_a_otimizado.json' in content or not content"</automated>
  </verify>
  <done>O arquivo antigo não existe mais, e o teleprompter.py usa os caminhos do Modo A.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Injetar classes e adaptadores do Modo B (D-01, D-02, D-05, D-06, D-09)</name>
  <files>signatures.py</files>
  <behavior>
    - AvaliacaoModoB herda de Avaliacao e possui defeitos_encontrados (list[str]).
    - AvaliadorModoB herda de dspy.Signature e possui defeitos_encontrados (dspy.OutputField) logo acima das notas.
    - load_avaliador() carrega AMBOS os modelos por padrão (Modo A e Modo B).
    - _invoke_judge_modo_b_with() mapeia a saída de AvaliadorModoB.
  </behavior>
  <action>
  1. Crie a classe `AvaliacaoModoB(Avaliacao)` estendendo o contrato base (D-06). Adicione o campo `defeitos_encontrados: list[str]`.
  2. Crie a classe `AvaliadorModoB(dspy.Signature)` copiando todos os campos de `AvaliadorDeSkill` (D-01). Insira um novo `dspy.OutputField` chamado `defeitos_encontrados` logo após `manteve_regras_criticas`, e antes de `nota_clareza` (D-02, D-05).
  3. Atualize `load_avaliador()`. Altere a função para SEMPRE carregar ambos os modelos nas suas respectivas variáveis globais (`avaliador_module` carrega `avaliador_modo_a_otimizado.json` e `avaliador_modo_b_module` carrega `avaliador_modo_b_otimizado.json`). Isso garante que `optimizer.py` e outras partes do sistema que invocam a função sem argumentos inicializem o Modo A corretamente, evitando degradação silenciosa do MCTS para zero-shot.
  4. Crie a função `_invoke_judge_modo_b_with(module, exemplo, predicao) -> AvaliacaoModoB`. Espelhe a lógica de `_invoke_judge_with`, mas retorne `AvaliacaoModoB`. Inclua lógica para transformar `res.defeitos_encontrados` (se string, quebrar por linhas ou vírgulas) em `list[str]`.
  </action>
  <verify>
    <automated>python -c "from signatures import AvaliacaoModoB, AvaliadorModoB, _invoke_judge_modo_b_with; assert 'defeitos_encontrados' in AvaliacaoModoB.model_fields"</automated>
  </verify>
  <done>O sistema de assinaturas DSPy possui isolamento completo para o Modo B e mantém a inicialização do Modo A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSPy→AvaliacaoModoB | Saída não estruturada do LLM é forçada para o modelo de validação Pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | AvaliacaoModoB | low | mitigate | Validar parsing da string bruta de defeitos_encontrados para list[str] dentro de _invoke_judge_modo_b_with |
</threat_model>

<verification>
python -c "from signatures import AvaliacaoModoB" roda sem erros de import.
</verification>

<success_criteria>
O modelo Pydantic e a assinatura DSPy coexistem com as estruturas do Modo A, sem quebrá-las.
</success_criteria>

<output>
Create `.planning/phases/02-judge-ca-a-defeitos-mode/02-01-SUMMARY.md` when done
</output>
