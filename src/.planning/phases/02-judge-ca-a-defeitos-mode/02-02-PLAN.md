---
phase: 02-judge-ca-a-defeitos-mode
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - drift/runner.py
  - ausculta_modo_b.py
autonomous: true
requirements: [JUD-01, JUD-02]

must_haves:
  truths:
    - O DriftRunner suporta a avaliação com o Modo B isoladamente (D-04).
    - O Golden Set do Modo B possui testes que validam cenários contraditórios (Espelho Distorcido).
    - A pipeline falha adequadamente a skill "Espelho Distorcido" com nota em torno de 0.665.
  prohibitions:
    - D-08 (Calibração do DriftGate): Os thresholds de DriftThresholds (spearman_floor, offset_alarm) não são alterados — a recalibração acontece nas expectativas dos probes, não nos limites do gate.
    - D-11 (Destino do Modelo Otimizado): A API de jobs (routers/jobs.py) não expõe o conceito de modo de avaliação. O modo é um detalhe interno do pipeline. O default Modo B é aplicado silenciosamente sem parâmetros externos.
  artifacts:
    - ausculta_modo_b.py
  key_links:
    - JudgeProbeRunner.run_modo_b utiliza _invoke_judge_modo_b_with.
---

<objective>
Integrar o Modo B ("Caça-Defeitos") ao pipeline de avaliação (`DriftRunner`) e criar um conjunto de testes Golden focado em reprovar ambiguidades e paradoxos estruturais.

Purpose: Testar o juiz em cenários em que o Modo A falhava (Elogiar texto bonito mas defeituoso).
Output: `run_modo_b` adicionado e script `ausculta_modo_b.py` funcional com skill "Espelho Distorcido".
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@drift/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Habilitar run_modo_b no DriftRunner e tornar Modo B o padrão (D-03, D-04, D-10)</name>
  <files>drift/runner.py</files>
  <behavior>
    - JudgeProbeRunner.__init__ também inicializa um juiz para o Modo B fresh.
    - JudgeProbeRunner.run_modo_b() usa _invoke_judge_modo_b_with e retorna ProbeMeasurement.
    - JudgeProbeRunner.run() é redirecionado para usar o Modo B por padrão (D-03).
  </behavior>
  <action>
  Atualize o `JudgeProbeRunner` no `drift/runner.py`:
  1. No `__init__`, adicione `self._judge_modo_b = dspy.Predict(AvaliadorModoB)`. Mantenha o `self._judge` do Modo A intacto (D-10).
  2. Implemente os equivalentes de carga: `load_candidate_modo_b(self, path)` e `as_zero_modo_b(self)`.
  3. Adicione `run_modo_b(self, probe, repetitions) -> ProbeMeasurement`. Esta função invoca `_invoke_judge_modo_b_with(self._judge_modo_b, exemplo, predicao)` (D-04).
  4. Para implementar integralmente a decisão D-03 ("Por padrão, toda avaliação usa o Modo B"), altere o método `run()` original para que ele chame internamente a lógica do Modo B por padrão. O Modo A deve ser preservado via um método `run_modo_a()` ou parâmetro `modo='a'` para atuar como fallback/debug.
  Importe os métodos e classes recém-criados de `signatures.py`.
  </action>
  <verify>
    <automated>python -c "from drift.runner import JudgeProbeRunner; assert hasattr(JudgeProbeRunner('test'), 'run_modo_b')"</automated>
  </verify>
  <done>O DriftRunner expõe a capacidade de medir o Modo B autonomamente e o adota como padrão.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Golden Set Modo B com "Espelho Distorcido" (D-07)</name>
  <files>ausculta_modo_b.py</files>
  <action>
  Crie o arquivo `ausculta_modo_b.py` do zero. Ele deve possuir a mesma mecânica de invocação de pipeline que `ausculta.py`, porém:
  1. Instancie e execute explicitamente com o método `.run_modo_b()` do `JudgeProbeRunner`.
  2. Defina os dados hardcoded (Golden Set) com pelo menos um Probe chave, que representa a skill "Espelho Distorcido" (uma skill que manda fazer algo e ao mesmo tempo restringe totalmente a mesma coisa de ser feita).
  3. No "Espelho Distorcido", a expectativa de sucesso `expected_composite` deve ser `0.665` (aproximadamente, indicando reprovação), já que o AvaliadorModoB deve detectar a contradição no seu campo `defeitos_encontrados`. Defina a `expected_rank_band='fail'` para este caso.
  4. O script deve construir o `DriftReport` e validar que as falhas são reportadas corretamente.
  Este script existirá ao lado de `ausculta.py` e fará o teste pontual do novo modelo.
  </action>
  <verify>
    <automated>python ausculta_modo_b.py</automated>
  </verify>
  <done>O Golden Set dedicado consegue reprovar uma skill contraditória que antes passaria ilesa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ausculta_modo_b.py | Test environment bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Info Disclosure | ausculta_modo_b.py | low | accept | Scripts de teste não requerem mitigação profunda, assumindo execução local sem credenciais perigosas vazadas. |
</threat_model>

<verification>
python ausculta_modo_b.py passa com sucesso e valida que o score foi penalizado.
</verification>

<success_criteria>
O Modo B detecta a contradição da skill do Espelho Distorcido e diminui sua nota compostamente.
</success_criteria>

<output>
Create `.planning/phases/02-judge-ca-a-defeitos-mode/02-02-SUMMARY.md` when done
</output>
