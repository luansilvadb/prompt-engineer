---
phase: 01-architectural-cleanup-densification
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-04]
files_modified:
  - src/drift_monitor.py
  - src/mutations.py
  - src/optimizer.py
  - src/teleprompter.py
  - src/services.py
  - src/api.py
autonomous: true
requirements: [ARC-01, ARC-03]
must_haves:
  truths:
    - "src/drift_monitor.py is now a thin re-export shim: it imports the full public surface from src.drift.* and contains NO logic bodies (no class/function definitions beyond re-export statements)"
    - "src/mutations.py is now a thin re-export shim importing from src.mutation_strategies.* with NO logic bodies"
    - "Every previously-working import path still resolves: from src.drift_monitor import DriftGate, GoldenSet, medir_drift, save_drift_cache, ... AND from src.mutations import registry, MutationBandit, get_mutation_prompt, ..."
    - "optimizer.py, teleprompter.py, services.py, api.py contain no unused imports, unreferenced module-level vars, or orphan functions (ARC-01)"
    - "The FastAPI app object imports and instantiates without error; the MCTS optimizer module and its key classes import without error"
  artifacts:
    - src/drift_monitor.py (rewritten to shim)
    - src/mutations.py (rewritten to shim)
  key_links:
    - "drift_monitor.py re-export <-> src.drift.{exceptions,models,golden,runner,metrics,gate,circuit_breaker,cache}"
    - "mutations.py re-export <-> src.mutation_strategies.{registry,bandit,api}"
---

<objective>
Wire the new packages into the codebase: convert the two old monolith files (`src/drift_monitor.py`, `src/mutations.py`) into backward-compatible re-export shims so existing imports keep working, run the ARC-01 dead-code sweep on the four consumer files, and perform the final integration verification (import smoke through BOTH old and new paths, FastAPI app instantiation, MCTS module import).

Purpose: ARC-03 backward-compatibility guarantee (REQUIREMENTS.md Out-of-Scope: "no API endpoint breakage") + ARC-01 completion. This is the plan that makes the refactor actually live — before it, the old files still contain duplicated logic; after it, the old paths are facades and the new packages are the single source of truth.
Output: Two rewritten shim files + four pruned consumer files + a green integration check.
</objective>

<execution_context>
@D:/good/src/.opencode/gsd-core/workflows/execute-plan.md
@D:/good/src/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architectural-cleanup-densification/01-CONTEXT.md
@.planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md
@.planning/phases/01-architectural-cleanup-densification/01-02-SUMMARY.md
@.planning/phases/01-architectural-cleanup-densification/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert src/drift_monitor.py and src/mutations.py into re-export shims</name>
  <files>src/drift_monitor.py, src/mutations.py</files>
  <read_first>
    - src/drift_monitor.py (the WHOLE current file — you are replacing its body; first inventory every public symbol so the shim re-exports the complete surface)
    - src/mutations.py (the WHOLE current file — same)
    - src/teleprompter.py (lines 8-17 — confirms the exact drift_monitor symbols a consumer imports: DriftGate, DriftMeasurementError, DriftThresholds, GoldenSet, JudgeProbeRunner, load_drift_cache, medir_drift, save_drift_cache)
    - src/routers/jobs.py (lines ~204-208 — confirms drift_monitor symbols: DriftThresholds, circuit_breaker, verificar_juiz_atual)
    - src/optimizer.py (lines ~29, ~138, ~234, ~249, ~257, ~258, ~424, ~432 — confirms mutations symbols: MutationBandit, registry, get_mutation_prompt, get_strategy_description)
    - test_discoverer.py (line ~5 — confirms `from src.mutations import registry`)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (sections "src/drift_monitor.py (modified → compatibility shim)" and "src/mutations.py (modified → compatibility shim)" — the exact __all__ lists and re-export statements)
    - All src/drift/*.py and src/mutation_strategies/*.py (plans 01-01..01-04 outputs — confirm the exact symbols each exports)
  </read_first>
  <action>
    REWRITE src/drift_monitor.py (preserve the filename). Replace its entire body with a re-export façade: a module docstring noting it is a compatibility shim (Phase 1 densification) and that implementation now lives under src/drift/, followed by absolute-import re-exports covering the COMPLETE public surface. The re-export set MUST include at least: DriftMeasurementError (from src.drift.exceptions); ProbeExpectation, GoldenProbe, DimensionError, DriftReport, GateDecision, DriftThresholds, ProbeMeasurement (from src.drift.models); GoldenSet, GOLDEN_DIR (from src.drift.golden); JudgeProbeRunner (from src.drift.runner); medir_drift, _spearman_rank_correlation (from src.drift.metrics); DriftGate (from src.drift.gate); verificar_juiz_atual, circuit_breaker, MODELS_DIR (from src.drift.circuit_breaker); load_drift_cache, save_drift_cache, _drift_cache_path (from src.drift.cache). Define __all__ listing every re-exported name. The file must contain NO class or function definitions and NO business logic — only imports and the __all__ list. (Note: the dead nested helper from old drift_monitor.py is intentionally NOT re-exported — it was deleted in plan 01-04.)

    REWRITE src/mutations.py (preserve the filename). Replace its entire body with a re-export façade: docstring noting compatibility shim with implementation under src.mutation_strategies/, then re-exports: StrategyRegistry, registry (from src.mutation_strategies.registry); MutationBandit (from src.mutation_strategies.bandit); get_mutation_prompt, get_strategy_description (from src.mutation_strategies.api). Define __all__ = ['StrategyRegistry', 'registry', 'MutationBandit', 'get_mutation_prompt', 'get_strategy_description']. No logic bodies.

    Do NOT create __init__.py anywhere. Use absolute imports rooted at src (never relative).
  </action>
  <acceptance_criteria>
    - Source assertion: src/drift_monitor.py contains `from src.drift.gate import DriftGate` and `from src.drift.cache import load_drift_cache, save_drift_cache` and `from src.drift.circuit_breaker import verificar_juiz_atual, circuit_breaker`
    - Source assertion: src/drift_monitor.py contains NO `class ` definition and NO top-level `def ` (it is a pure shim) — grep -cE '^(class |def )' returns 0
    - Source assertion: src/mutations.py contains `from src.mutation_strategies.registry import` and `from src.mutation_strategies.bandit import MutationBandit` and `from src.mutation_strategies.api import`
    - Source assertion: src/mutations.py contains NO `class ` definition and NO top-level `def ` — grep -cE '^(class |def )' returns 0
    - Source assertion: neither shim uses a relative import (grep `from \.` returns 0 in both)
    - OLD PATH RESOLVES (drift): `python -c "from src.drift_monitor import DriftGate, GoldenSet, medir_drift, save_drift_cache, load_drift_cache, JudgeProbeRunner, DriftMeasurementError, DriftThresholds, circuit_breaker, verificar_juiz_atual; print('drift shim ok')"` exits 0
    - OLD PATH RESOLVES (mutations): `python -c "from src.mutations import registry, MutationBandit, get_mutation_prompt, get_strategy_description, StrategyRegistry; print('mutations shim ok')"` exits 0
    - NEW PATH RESOLVES: `python -c "from src.drift.gate import DriftGate; from src.mutation_strategies.bandit import MutationBandit; print('new paths ok')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.drift_monitor import DriftGate, GoldenSet, medir_drift, save_drift_cache, load_drift_cache, JudgeProbeRunner, DriftMeasurementError, DriftThresholds, circuit_breaker, verificar_juiz_atual; from src.mutations import registry, MutationBandit, get_mutation_prompt, get_strategy_description; print('shims ok')"</automated>
  </verify>
  <done>Both old files are pure re-export shims (no logic bodies); every historically-imported symbol resolves through the old path AND the new package path; backward compatibility preserved.</done>
</task>

<task type="auto">
  <name>Task 2: ARC-01 dead-code sweep on the four consumer files</name>
  <files>src/optimizer.py, src/teleprompter.py, src/services.py, src/api.py</files>
  <read_first>
    - src/optimizer.py (full file — inventory imports at L24-30 and every module-level name; find any imported symbol used only on its import line, any unreferenced module-level variable, any orphan function never called)
    - src/teleprompter.py (full file — same inventory; note its multi-line parenthesized import from src.drift_monitor at L8-17)
    - src/services.py (full file)
    - src/api.py (full file)
    - .planning/phases/01-architectural-cleanup-densification/01-PATTERNS.md (section "src/optimizer.py, src/teleprompter.py, src/services.py, src/api.py (modified — ARC-01 dead code pass)")
  </read_first>
  <action>
    For EACH of optimizer.py, teleprompter.py, services.py, api.py: identify and DELETE dead code — (a) imports whose symbol is never referenced anywhere else in that same file (verify by grepping the symbol name within the file; a symbol appearing ONLY on its import line is dead), (b) module-level variables assigned but never read, (c) functions defined but never called anywhere in the repo (verify repo-wide with grep before deleting; a function with only its own `def` line as a match is orphan). Do NOT change any kept symbol's name, signature, or behavior. Preserve the existing import style: absolute `from src.X import (...)` parenthesized multi-line form for multi-symbol imports (Shared Pattern #2). Do NOT touch src/drift_monitor.py or src/mutations.py (those are now shims from Task 1) — the dead-code sweep is on the four CONSUMER files only. The drift/mutations split does not change these files' imports because the shims preserve the paths; only genuine pre-existing dead code is removed.

    If any candidate deletion is ambiguous (e.g., a function that might be invoked via getattr/string dispatch, or referenced in a non-obvious way), LEAVE it in place and note it — do not risk breaking the API (Out-of-Scope: no endpoint breakage).
  </action>
  <acceptance_criteria>
    - For each pruned import, the symbol genuinely has no other occurrence in its file (manually confirmed during the task; the executor re-runs a per-symbol grep after deletion to confirm zero remaining references that would now be undefined).
    - Import smoke per file: `python -c "import src.optimizer, src.teleprompter, src.services, src.api; print('consumers import ok')"` exits 0 (proves no NameError was introduced by over-pruning)
    - Compile check: `python -m compileall -q src/optimizer.py src/teleprompter.py src/services.py src/api.py` exits 0
    - No new behavior: diff contains only deletions of provably-unused imports/vars/functions (no logic edits)
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall -q src/optimizer.py src/teleprompter.py src/services.py src/api.py
python -c "import src.optimizer, src.teleprompter, src.services, src.api; print('arc-01 sweep ok')"</automated>
  </verify>
  <done>The four consumer files contain no unused imports, no unreferenced module-level vars, no orphan functions; all four still import and compile cleanly; only deletions were made.</done>
</task>

<task type="auto">
  <name>Task 3: Final integration verification — import smoke + FastAPI + MCTS module</name>
  <files>(none — read-only verification gate; no source edits)</files>
  <read_first>
    - src/api.py (confirm how the FastAPI app is constructed — the app object name, e.g. `app` or returned by a factory)
    - src/optimizer.py (confirm the MCTS entry classes — MCTSOptimizer or similar — that must import cleanly)
    - src/routers/jobs.py and src/routers/frontend.py (confirm router registration so the app-instantiation check is meaningful)
    - All PLAN SUMMARYs 01-01..01-04 (the canonical symbol homes)
  </read_first>
  <action>
    Run the integration verification battery (all must pass). These are read-only checks — no source edits in this task:
      1. Whole-package compile: `python -m compileall -q src` exits 0.
      2. Full public-surface import through BOTH paths in one process:
         `python -c "from src.drift_monitor import DriftGate, GoldenSet, medir_drift, save_drift_cache, load_drift_cache, JudgeProbeRunner, DriftMeasurementError, DriftThresholds, circuit_breaker, verificar_juiz_atual; from src.drift.gate import DriftGate as G2; from src.mutations import registry, MutationBandit, get_mutation_prompt, get_strategy_description; from src.mutation_strategies.bandit import MutationBandit as B2; assert DriftGate is G2; assert MutationBandit is B2; print('identity ok')"` — the `is` checks prove the shim re-exports point at the SAME objects (not copies), confirming single-source-of-truth.
      3. FastAPI app instantiation (no network, no LLM): `python -c "import src.api as m; app = getattr(m, 'app', None) or m.create_app(); routes = [r.path for r in app.routes]; assert routes, 'no routes registered'; print('fastapi ok', len(routes), 'routes')"` exits 0 — proves the app object builds and routers register without import-time errors induced by the refactor.
      4. MCTS module import + key class instantiation (no LLM call): `python -c "import src.optimizer; from src.mutations import MutationBandit; b=MutationBandit(); assert isinstance(b.select(), str); print('mcts module ok')"` exits 0.
      5. Repo-wide dead-helper absence (ARC-01 final): `grep -rn "_strict_better_or_reject" src/` returns ZERO matches (the old drift_monitor.py copy is gone because the file is now a shim, and gate.py never had it).
      6. Namespace-package invariant: confirm NO __init__.py was created under src/drift/ or src/mutation_strategies/ (glob `src/drift/__init__.py`, `src/mutation_strategies/__init__.py` both absent).
      7. ARC-01 codebase-wide unused-symbol visibility scan (SC1 backstop, per revision): run `ruff check --select F401,F841 src/` if ruff is installed (preferred — precise F401 unused-import + F841 unused-local-variable selection), otherwise `python -m pyflakes src/` (fallback). If NEITHER is available, install ruff (`pip install ruff`) before proceeding — the scan MUST run. This machine-checks ALL source files (not just the four manually-swept consumers) for dead imports and unused variables, turning Success Criterion #1 ("Codebase livre de variáveis inativas, funções órfãs e imports desnecessários") from a 4-file sample into a codebase-wide, machine-checkable gate. PURPOSE IS VISIBILITY (per checker note): pre-existing findings in files NOT touched by this phase (e.g. signatures.py, config.py, store.py, value_estimator.py, experience_store.py, schemas.py, state.py, ausculta.py, routers/jobs.py, routers/frontend.py) are acceptable and informative — LOG them in the plan summary as pre-existing debt; findings in the four swept consumer files (optimizer/teleprompter/services/api) MUST be zero because Task 2 manually cleaned them. Do NOT widen the manual sweep beyond the four files — this automated scan is the backstop.

    If any check fails, STOP and report which check failed with the exact error — do not attempt to mask failures. (A full live MCTS optimization run requires an LLM API key and real network calls; it is intentionally OUT of this automated gate and belongs to human UAT — note this in the summary.)
  </action>
  <acceptance_criteria>
    - `python -m compileall -q src` exits 0
    - The combined old+new-path import one-liner exits 0 AND prints "identity ok" (shim re-exports are the same objects as the package originals)
    - The FastAPI app-instantiation one-liner exits 0 AND prints a route count > 0
    - The MCTS module-import one-liner exits 0 AND MutationBandit().select() returns a str
    - `grep -rn "_strict_better_or_reject" src/` returns 0 matches (final ARC-01 confirmation)
    - Glob confirms src/drift/__init__.py and src/mutation_strategies/__init__.py do NOT exist
    - ARC-01 / SC1 codebase-wide unused-symbol scan EXECUTES over src/ (ruff `--select F401,F841`, or `python -m pyflakes src/` fallback): the scan runs to completion and its output is reviewed; the four swept consumer files (optimizer/teleprompter/services/api) report ZERO F401/F841 findings, while pre-existing findings in untouched files are logged in the summary as debt (visibility gate, not a zero-findings gate)
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall -q src
python -c "from src.drift_monitor import DriftGate, GoldenSet, medir_drift, save_drift_cache; from src.drift.gate import DriftGate as G2; from src.mutations import registry, MutationBandit; from src.mutation_strategies.bandit import MutationBandit as B2; assert DriftGate is G2 and MutationBandit is B2; import src.api as m; app=getattr(m,'app',None) or m.create_app(); assert len([r.path for r in app.routes])>0; assert isinstance(MutationBandit().select(),str); print('integration green')"
if (Get-Command ruff -ErrorAction SilentlyContinue) { ruff check --select F401,F841 src/ } else { python -m pyflakes src/ }; 'ARC-01 codebase-wide unused-symbol scan complete — review findings above (visibility gate; swept consumer files must be clean)'</automated>
  </verify>
  <done>All seven integration checks pass: whole-package compiles; old and new import paths resolve to identical objects; FastAPI app builds with routes registered; MCTS module imports and MutationBandit selects; the dead helper is absent repo-wide; no __init__.py was introduced; the ARC-01/SC1 codebase-wide unused-symbol scan (ruff F401/F841 or pyflakes fallback) executed over src/ with the swept consumer files clean and any pre-existing findings elsewhere logged.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
Modified (not new) — the shim files and pruned consumers:
- src/drift_monitor.py — rewritten to a pure re-export façade (no logic)
- src/mutations.py — rewritten to a pure re-export façade (no logic)
- src/optimizer.py, src/teleprompter.py, src/services.py, src/api.py — dead imports/vars/orphan functions removed (ARC-01)
No NEW public symbols are introduced by this plan (it only wires existing ones and prunes). The phase's complete new-symbol inventory lives in plans 01-01 through 01-04.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries
This plan changes how existing code is reached (shim re-exports) but introduces NO new trust boundary, NO new input source, NO new dependency. Backward compatibility is the security-relevant property: a broken shim would surface as an ImportError at startup (fail-closed), not a silent security regression.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | shim re-export surface | medium | mitigate | Task 1 acceptance criteria require the FULL public surface to be re-exported (grep-verified against consumer import lines in teleprompter.py L8-17 and routers/jobs.py L204-208); Task 3 verifies old+new paths resolve to identical objects (`is` check). A missing re-export fails the integration gate loudly. |
| T-01-08 | Denial of Service | ARC-01 over-pruning (deleting a used symbol) | medium | mitigate | Task 2 requires per-symbol grep confirmation before each deletion and a post-deletion `import` + `compileall` smoke per file; ambiguous candidates are LEFT in place (no endpoint breakage per Out-of-Scope). |
| T-01-09 | Information Disclosure | dead-helper removal audit trail | low | accept | Removal is of provably-uncalled code (repo-wide grep confirms zero callers); git history retains recoverability (CONTEXT.md D-03). |
| T-01-SC | Tampering | package installs | low | accept | No package-manager installs in this plan. No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
Phase-level integration gate (Task 3 above) — all seven checks green:
- compileall src clean
- old + new import paths resolve to identical objects
- FastAPI app builds, routes > 0
- MCTS module imports, MutationBandit.select() returns str
- dead helper absent repo-wide
- no __init__.py introduced
- ARC-01/SC1 codebase-wide unused-symbol scan (ruff F401/F841 or pyflakes fallback) executed over src/ — visibility gate; swept consumer files clean, pre-existing findings elsewhere logged

NOTE on Success Criterion #3 ("FastAPI and MCTS execute without error or slowness"): this plan proves structural integrity (imports, app build, class instantiation, compile). A full LIVE MCTS optimization run requires an LLM API key + network and is deferred to human UAT (out of automated scope). The refactor is behavior-preserving by construction (relocations + dead-code removal of uncalled code), so no latency regression is expected.
</verification>

<success_criteria>
- Both old monolith files are pure shims; backward-compatible import paths intact (ARC-03).
- Four consumer files pruned of dead code (ARC-01).
- Integration gate fully green: compile, dual-path identity, FastAPI app, MCTS module import.
- Dead helper absent repo-wide; namespace-package invariant (no __init__.py) intact.
- Phase 1 Success Criteria #1 (no dead code), #2 (complex branches extracted — delivered in plans 01-02/03/04), #3 (FastAPI + MCTS run without refactor-induced error — structurally proven) all addressed.
</success_criteria>

<output>
Create `.planning/phases/01-architectural-cleanup-densification/01-05-SUMMARY.md` when done
</output>
