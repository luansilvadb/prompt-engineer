# Edge Case Hunter Review Prompt
# Generated by bmad-quick-dev step-04

Invoke the `bmad-review-edge-case-hunter` skill on this diff summary:

## Changes Summary

**51 files changed, +35 / -1,279 lines (net -1,244)**

### Structural changes:
1. **Deleted**: `nul` (Windows artifact), `.pytest_tmp/` (pytest temp files)
2. **Deleted**: `src/mutation_strategies/bandit_interfaces.py` — 1-line re-export shim
3. **Moved**: `desktop.py` → `scripts/desktop.py`, `SkillOptimizer.spec` → `scripts/SkillOptimizer.spec`
4. **Consolidated**: 4 evaluator modules into `src/evaluators/` package
5. **Deleted**: `src/infrastructure/experimental/` (non-production code)
6. **Refactored**: `src/experience_store.py` — removed JSON `ExperienceStore` class
7. **Updated**: `src/teleprompter.py` — uses `create_experience_store()` (SQLite) instead of `ExperienceStore()` (JSON)

### Edge cases to hunt:
- What happens when someone has stale `.pyc` files from old evaluator paths?
- Does `SqliteExperienceStore.experiences` property work identically to the old `ExperienceStore.experiences` list?
- Are there any lazy imports that might break at runtime (not import time)?
- Does `src/evaluators/__init__.py` re-export everything consumers need?
- Is the `_has_structured_fields` private import from tests still valid?
- Does `scripts/desktop.py` still import `src.api` correctly from its new location?
- Are any test files still referencing old paths not caught by the grep sweep?
