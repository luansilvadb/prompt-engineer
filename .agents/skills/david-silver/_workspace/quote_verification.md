# Quote verification

Verified 52/65 (80%).

## Unverified

These quotes didn't match any of their listed source texts and were stripped from the corpus before authoring.

- **principles** / _RLHF is Fundamentally Ungrounded_ (sources: src_013, src_019) — match ratio 0.77
  > I want to say that when we train a system from human feedback that it is not grounded... it's like the human is prejudging the output of the system.
- **principles** / _Creativity is Trial-and-Error Learning_ (sources: src_004) — match ratio 0.69
  > The whole process of trial-and-error learning... is a process of creativity. ... The process is a million mini discoveries, one after the other. It is the essence of creativity.
- **principles** / _RL Breaks the IID Assumption_ (sources: src_015) — match ratio 0.81
  > we're not talking about IID data here... the agent is actually influencing the data that it sees this is like an active learning process
- **frameworks** / _Zero-Knowledge Self-Play Loop (AlphaZero)_ (sources: src_014, src_004, src_013, src_019) — match ratio 0.59
  > you take the best move according to your search and you train your policy to do more things like that... and you train your value function based on how the game actually panned out... You just iterate that millions of times and out pops a superhuman game player.
- **frameworks** / _Autonomous RL Rule Discovery (DiscoRL)_ (sources: src_050) — match ratio 0.80
  > During the discovery process, we instantiate a population of agents, each of which interacts with its own instance of an environment taken from a diverse set of challenging tasks... We then use the meta-gradient method to incrementally improve the RL rule.
- **frameworks** / _RL Agent Decomposition_ (sources: src_020) — match ratio 0.79
  > how do you solve a really hard problem like that? Well, one approach you can take is, is to decompose that that very hard problem into into pieces... whether or not that solution has a value function... a policy... a model...
- **frameworks** / _RL Agent Taxonomy_ (sources: src_015) — match ratio 0.77
  > an RL agent may include one of these three things these are the main objects for reinforcement... a policy... a value function... a model
- **mental_models** / _Fossil Fuels vs. Sustainable Energy_ (sources: src_014, src_013, src_019) — match ratio 0.61
  > human data might give us a head start. It's a bit like to to borrow a metaphor fossil fuels... And I think that's what reinforcement learning is. It's a sustainable fuel.
- **mental_models** / _State as a Summary of History_ (sources: src_015) — match ratio 0.70
  > the history isn't very useful useful because it's typically enormous... and we don't want to have to go back to this history every time
- **mental_models** / _The Cake Recipe Grounding Metaphor_ (sources: src_013) — match ratio 0.77
  > I want to say that when we train a system from human feedback that it is not grounded... it's like the human is prejudging the output of the system.
- **signature_quotes** / _RL as sustainable fuel_ (sources: src_013, src_019) — match ratio 0.61
  > human data might give us a head start. It's a bit like to to borrow a metaphor fossil fuels... And I think that's what reinforcement learning is. It's a sustainable fuel.
- **signature_quotes** / _Ungrounded RLHF_ (sources: src_013, src_019) — match ratio 0.77
  > I want to say that when we train a system from human feedback that it is not grounded... it's like the human is prejudging the output of the system.
- **signature_quotes** / _Model-Free vs Model-Based_ (sources: src_015) — match ratio 0.78
  > perhaps the fundamental distinction in RL is between model free and modelbased reinforcement learning
